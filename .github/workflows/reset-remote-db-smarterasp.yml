name: Reset Remote DB (SmarterASP)

on:
  workflow_dispatch: {}

jobs:
  reset-db:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        working-directory: api-core
        run: dotnet restore

      - name: Install dotnet-ef
        shell: pwsh
        run: |
          dotnet tool install --global dotnet-ef --version 8.*
          "$env:USERPROFILE\.dotnet\tools" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: DROP tables (customers, __EFMigrationsHistory)
        shell: pwsh
        run: |
          $conn = "${{ secrets.SMARTERASP_DB_CONNECTIONSTRING }}"
          if ([string]::IsNullOrWhiteSpace($conn)) {
            throw "SMARTERASP_DB_CONNECTIONSTRING não está definido."
          }

          $sql = @"
IF OBJECT_ID(N'[customers]') IS NOT NULL DROP TABLE [customers];
IF OBJECT_ID(N'[__EFMigrationsHistory]') IS NOT NULL DROP TABLE [__EFMigrationsHistory];
"@

          $sqlConn = New-Object System.Data.SqlClient.SqlConnection($conn)
          $sqlConn.Open()
          try {
            $cmd = $sqlConn.CreateCommand()
            $cmd.CommandTimeout = 120
            $cmd.CommandText = $sql
            [void]$cmd.ExecuteNonQuery()
          }
          finally {
            $sqlConn.Close()
          }

      - name: Apply EF Core Migrations
        shell: pwsh
        run: |
          $conn = "${{ secrets.SMARTERASP_DB_CONNECTIONSTRING }}"
          if ([string]::IsNullOrWhiteSpace($conn)) {
            throw "SMARTERASP_DB_CONNECTIONSTRING não está definido."
          }

          dotnet ef database update `
            --project "api-core/src/Diax.Infrastructure/Diax.Infrastructure.csproj" `
            --startup-project "api-core/src/Diax.Api/Diax.Api.csproj" `
            --connection "$conn"
