---
phase: 02-funda-o-de-agentes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-core/src/Diax.Domain/Agents/AgentPendingAction.cs
  - api-core/src/Diax.Domain/Agents/IAgentPendingActionRepository.cs
  - api-core/src/Diax.Domain/AiChat/AiConversation.cs
  - api-core/src/Diax.Domain/AiChat/IAiChatRepository.cs
  - api-core/src/Diax.Infrastructure/Data/Repositories/AgentPendingActionRepository.cs
  - api-core/src/Diax.Infrastructure/Data/Repositories/AiChatRepository.cs
  - api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs
  - api-core/src/Diax.Infrastructure/DependencyInjection.cs
  - api-core/tests/Diax.Tests/Application/Agents/AgentPendingActionTests.cs
autonomous: true
requirements: [ORCH-04, ORCH-05]
must_haves:
  truths:
    - "AgentPendingAction.IsPending is false once executed, cancelled, or expired"
    - "An agent conversation persists its AgentType; an AiChat conversation has AgentType == null"
    - "The agent_pending_actions table and agent_type column exist in the DB after migration"
  artifacts:
    - path: "api-core/src/Diax.Domain/Agents/AgentPendingAction.cs"
      provides: "Pending-action domain entity with state-machine invariants"
      contains: "public bool IsPending"
    - path: "api-core/src/Diax.Domain/Agents/IAgentPendingActionRepository.cs"
      provides: "Repository contract for pending actions"
      contains: "GetPendingByIdAsync"
    - path: "api-core/src/Diax.Infrastructure/Data/Repositories/AgentPendingActionRepository.cs"
      provides: "EF Core repository implementation"
    - path: "api-core/tests/Diax.Tests/Application/Agents/AgentPendingActionTests.cs"
      provides: "Domain invariant tests for pending action state machine"
      contains: "IsPending"
  key_links:
    - from: "AiConversation"
      to: "AgentType"
      via: "nullable column + SetAgentType()"
      pattern: "AgentType\\?"
    - from: "DiaxDbContext"
      to: "AgentPendingAction"
      via: "DbSet registration"
      pattern: "DbSet<AgentPendingAction>"
---

<objective>
Build the persistence foundation for the agent platform: the `AgentPendingAction` domain entity + repository (the source of truth for the confirmation flow, ORCH-04), and associate `AgentType` with `AiConversation` via a nullable column (ORCH-05). Two lightweight EF Core migrations applied via `update-db.ps1`.

Purpose: Phases 3-5 plug write-tools into this. The pending-action row is what guarantees NO data is written before `/confirm`. The nullable AgentType column separates agent conversations from existing AiChat conversations without touching existing rows.
Output: New domain entity, repository (interface + impl), AiConversation change, two migrations, DbSet + DI registration, and domain invariant tests.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-funda-o-de-agentes/02-CONTEXT.md
@.planning/phases/02-funda-o-de-agentes/02-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor must implement against. Extracted from codebase 2026-05-28. -->

AgentType enum (Diax.Domain/Agents/AgentType.cs — DO NOT CHANGE):
```csharp
public enum AgentType { Personal = 0, Support = 1, Commercial = 2 }
```

AiConversation today (Diax.Domain/AiChat/AiConversation.cs) — extends AuditableEntity, IUserOwnedEntity.
Existing setter pattern (all call SetUpdated()):
```csharp
public void SetModel(string model) { ...; Model = model.Trim(); SetUpdated(); }
```

IAiChatRepository (Diax.Domain/AiChat/IAiChatRepository.cs) — current signature to extend:
```csharp
Task<PagedResult<AiConversation>> GetPagedByUserAsync(
    Guid userId, int page, int pageSize, bool includeArchived = false, CancellationToken cancellationToken = default);
```

IRepository<T> base (Diax.Domain/Common/IRepository.cs): GetByIdAsync, AddAsync, UpdateAsync, GetPagedAsync exist.

Repository<T> base ctor pattern (AiChatRepository.cs): `public AiChatRepository(DiaxDbContext context) : base(context) {}` — exposes `DbSet` and `Context`.

DbContext DbSets (DiaxDbContext.cs lines 93-94):
```csharp
public DbSet<AiConversation> AiConversations => Set<AiConversation>();
public DbSet<AiChatMessage> AiChatMessages => Set<AiChatMessage>();
```

DI registration (Diax.Infrastructure/DependencyInjection.cs line 287):
```csharp
services.AddScoped<IAiChatRepository, AiChatRepository>();
```

Migration scripts (api-core/scripts/): `add-migration.ps1 -Name "X"` to generate, `update-db.ps1` to apply to PROD. DB is SQL Server (snake_case auto-mapping, DateTime forced UTC).

Tests run with: `DOTNET_ROLL_FORWARD=Major dotnet test api-core/tests/Diax.Tests/Diax.Tests.csproj --filter "FullyQualifiedName~Agents"` (machine has only .NET 10 runtime; project is net8.0).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AgentPendingAction domain entity + repository contract + invariant tests</name>
  <read_first>
    - api-core/src/Diax.Domain/Agents/AgentType.cs
    - api-core/src/Diax.Domain/Common/IRepository.cs
    - api-core/src/Diax.Domain/AiChat/IAiChatRepository.cs (repository contract style)
    - api-core/tests/Diax.Tests/Application/Agents/CommercialAgentServiceTests.cs (test style — xUnit + FluentAssertions)
    - .planning/phases/02-funda-o-de-agentes/02-RESEARCH.md (Pattern 5: AgentPendingAction)
  </read_first>
  <behavior>
    - New entity, no args ctor: a freshly created AgentPendingAction (ExpiresAt = now+15min) has IsPending == true and IsExpired == false.
    - MarkExecuted() sets ExecutedAt → IsPending becomes false.
    - Cancel() sets CancelledAt → IsPending becomes false.
    - An action whose ExpiresAt is in the past has IsExpired == true and IsPending == false.
  </behavior>
  <action>
    Create `api-core/src/Diax.Domain/Agents/AgentPendingAction.cs` exactly per RESEARCH Pattern 5. Class in namespace `Diax.Domain.Agents`. It does NOT need to extend AuditableEntity (it carries its own CreatedAt). Properties with private setters: Id (Guid), UserId, ConversationId, AgentType (Diax.Domain.Agents.AgentType), ToolName (string), ToolUseId (string), ActionLabel (string), Payload (string JSON), ExpiresAt (DateTime), CreatedAt (DateTime), ExecutedAt (DateTime?), CancelledAt (DateTime?).
    Add a private parameterless ctor for EF Core, and a public ctor:
    `public AgentPendingAction(Guid userId, Guid conversationId, AgentType agentType, string toolName, string toolUseId, string actionLabel, string payload, int ttlMinutes = 15)` that sets Id = Guid.NewGuid(), CreatedAt = DateTime.UtcNow, ExpiresAt = DateTime.UtcNow.AddMinutes(ttlMinutes), and validates toolName/toolUseId/payload not null/empty (throw ArgumentException).
    Computed properties: `public bool IsExpired => DateTime.UtcNow > ExpiresAt;` and `public bool IsPending => ExecutedAt is null && CancelledAt is null && !IsExpired;`.
    Methods: `public void MarkExecuted() { ExecutedAt = DateTime.UtcNow; }` and `public void Cancel() { CancelledAt = DateTime.UtcNow; }`.

    Create `api-core/src/Diax.Domain/Agents/IAgentPendingActionRepository.cs` in namespace `Diax.Domain.Agents`:
    ```csharp
    public interface IAgentPendingActionRepository
    {
        Task AddAsync(AgentPendingAction action, CancellationToken ct = default);
        Task<AgentPendingAction?> GetPendingByIdAsync(Guid id, Guid userId, CancellationToken ct = default);
        Task UpdateAsync(AgentPendingAction action, CancellationToken ct = default);
    }
    ```

    Create test file `api-core/tests/Diax.Tests/Application/Agents/AgentPendingActionTests.cs` (xUnit + FluentAssertions). Cover the four behaviors above. For the "expired" case construct with `ttlMinutes: -1` (ExpiresAt in past). Assert `IsPending` and `IsExpired` exactly.
  </action>
  <verify>
    <automated>cd api-core && DOTNET_ROLL_FORWARD=Major dotnet test tests/Diax.Tests/Diax.Tests.csproj --filter "FullyQualifiedName~AgentPendingActionTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public bool IsPending" api-core/src/Diax.Domain/Agents/AgentPendingAction.cs` succeeds.
    - `grep -q "public void MarkExecuted" api-core/src/Diax.Domain/Agents/AgentPendingAction.cs` succeeds.
    - `grep -q "GetPendingByIdAsync" api-core/src/Diax.Domain/Agents/IAgentPendingActionRepository.cs` succeeds.
    - AgentPendingActionTests has at least 4 [Fact] methods (`grep -c "\[Fact\]"` >= 4).
    - The filtered test command exits 0 with all tests passed.
  </acceptance_criteria>
  <done>AgentPendingAction entity + repository interface exist; 4+ invariant tests pass under the Agents filter.</done>
</task>

<task type="auto">
  <name>Task 2: AgentType on AiConversation + EF repository + DbSet + migrations</name>
  <read_first>
    - api-core/src/Diax.Domain/AiChat/AiConversation.cs
    - api-core/src/Diax.Domain/AiChat/IAiChatRepository.cs
    - api-core/src/Diax.Infrastructure/Data/Repositories/AiChatRepository.cs
    - api-core/src/Diax.Infrastructure/Data/Repositories/AgentPendingActionRepository.cs (sibling style — Repository<T> base)
    - api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs (lines 90-100 for DbSet section)
    - api-core/src/Diax.Infrastructure/DependencyInjection.cs (line 287 area, repository registrations)
    - api-core/scripts/add-migration.ps1 and update-db.ps1
    - .planning/phases/02-funda-o-de-agentes/02-RESEARCH.md (Pattern 6 + Pitfall 3)
  </read_first>
  <action>
    1. Edit `AiConversation.cs`: add `public AgentType? AgentType { get; private set; }` (using Diax.Domain.Agents) and method `public void SetAgentType(AgentType agentType) { AgentType = agentType; SetUpdated(); }`. Default stays null (existing AiChat conversations remain null — see Pitfall 3).

    2. Edit `IAiChatRepository.cs`: add an optional filter param to GetPagedByUserAsync — change signature to:
    `Task<PagedResult<AiConversation>> GetPagedByUserAsync(Guid userId, int page, int pageSize, bool includeArchived = false, AgentType? agentType = null, CancellationToken cancellationToken = default);`
    (using Diax.Domain.Agents). Null = no filter (preserves existing behavior). AiChatService already calls it WITHOUT agentType — that call still compiles via the default. Verify AiChatService.ListConversationsAsync call site still compiles (it passes positional args up to includeArchived then cancellationToken — confirm cancellationToken is passed by name or fix the call site to `GetPagedByUserAsync(userId, page, pageSize, includeArchived, cancellationToken: cancellationToken)`).

    3. Edit `AiChatRepository.cs` GetPagedByUserAsync: add the `AgentType? agentType = null` param; after the includeArchived filter add: `if (agentType.HasValue) query = query.Where(c => c.AgentType == agentType.Value); else query = query.Where(c => c.AgentType == null);` — wait: existing AiChat callers pass null and must keep seeing ALL conversations. So instead: only filter when agentType.HasValue: `if (agentType.HasValue) query = query.Where(c => c.AgentType == agentType.Value);`. When null, no AgentType filter (existing behavior preserved). Document this with a comment.

    4. Create `api-core/src/Diax.Infrastructure/Data/Repositories/AgentPendingActionRepository.cs`: `public class AgentPendingActionRepository : IAgentPendingActionRepository`. Inject `DiaxDbContext _db`. Implement: AddAsync → `await _db.Set<AgentPendingAction>().AddAsync(action, ct);`. GetPendingByIdAsync → `await _db.Set<AgentPendingAction>().FirstOrDefaultAsync(a => a.Id == id && a.UserId == userId, ct);`. UpdateAsync → `_db.Set<AgentPendingAction>().Update(action); return Task.CompletedTask;`. (No SaveChanges here — IUnitOfWork handles commit, same as AiChatRepository.)

    5. Edit `DiaxDbContext.cs`: add `public DbSet<AgentPendingAction> AgentPendingActions => Set<AgentPendingAction>();` near the AiConversations DbSets (line ~94). Add `using Diax.Domain.Agents;` if missing.

    6. Edit `Diax.Infrastructure/DependencyInjection.cs` near line 287: add `services.AddScoped<IAgentPendingActionRepository, AgentPendingActionRepository>();` (with appropriate usings).

    7. Generate the migration: run `cd api-core; .\scripts\add-migration.ps1 -Name "AddAgentFoundation"`. This produces ONE migration covering both the nullable `agent_type` column on `ai_conversations` AND the new `agent_pending_actions` table. Verify the generated migration: `agent_type` is `nullable: true` with NO defaultValue; the table has columns id, user_id, conversation_id, agent_type (int, NOT NULL), tool_name, tool_use_id, action_label, payload (nvarchar(max)), expires_at, created_at, executed_at (nullable), cancelled_at (nullable). If payload was not generated as nvarchar(max), add `.HasColumnType("nvarchar(max)")` configuration in the entity config / OnModelCreating or via `[Column(TypeName="nvarchar(max)")]`-equivalent fluent config.

    8. Apply to PROD: run `cd api-core; .\scripts\update-db.ps1`. Confirm output shows the migration applied with no errors.

    DO NOT run raw `dotnet ef database update`. Use the scripts only.
  </action>
  <verify>
    <automated>cd api-core && DOTNET_ROLL_FORWARD=Major dotnet build Diax.CRM.sln 2>&1 | Select-String -Pattern "Build succeeded|error"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AgentType? AgentType" api-core/src/Diax.Domain/AiChat/AiConversation.cs` and `grep -q "SetAgentType" ...` succeed.
    - `grep -q "AgentType? agentType" api-core/src/Diax.Domain/AiChat/IAiChatRepository.cs` succeeds.
    - `grep -q "DbSet<AgentPendingAction>" api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs` succeeds.
    - `grep -q "IAgentPendingActionRepository, AgentPendingActionRepository" api-core/src/Diax.Infrastructure/DependencyInjection.cs` succeeds.
    - A new migration file exists under `api-core/src/Diax.Infrastructure/Data/Migrations/` whose name ends with `AddAgentFoundation` and references both `agent_type` and `agent_pending_actions` (grep both strings in the migration `.cs`).
    - The generated migration sets `agent_type` column `nullable: true` (grep `nullable: true` near `agent_type`).
    - `dotnet build Diax.CRM.sln` reports Build succeeded with 0 errors (existing AiChatService call site still compiles).
    - `update-db.ps1` completes without error (migration applied to PROD).
  </acceptance_criteria>
  <done>AiConversation has nullable AgentType; pending-action repo + DbSet + DI registered; single AddAgentFoundation migration generated and applied to PROD via update-db.ps1; solution builds clean.</done>
</task>

</tasks>

<verification>
- `cd api-core && DOTNET_ROLL_FORWARD=Major dotnet test tests/Diax.Tests/Diax.Tests.csproj --filter "FullyQualifiedName~Agents"` — all pass (includes existing 4 CommercialAgentService tests + new AgentPendingAction tests).
- `dotnet build Diax.CRM.sln` — 0 errors.
- Migration applied to PROD without error.
</verification>

<success_criteria>
- AgentPendingAction entity exists with IsPending/IsExpired invariants tested.
- AiConversation carries nullable AgentType; existing conversations unaffected (null).
- agent_pending_actions table + agent_type column exist in PROD DB.
- No existing AiChat behavior changed; full build green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-funda-o-de-agentes/02-01-SUMMARY.md` documenting: exact migration name applied, AgentPendingAction public API, and the new GetPagedByUserAsync signature (so Plan 03 can call it with an AgentType filter).
</output>
