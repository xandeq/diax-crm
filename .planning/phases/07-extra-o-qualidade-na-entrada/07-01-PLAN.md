---
phase: 07-extra-o-qualidade-na-entrada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs
  - api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs
  - api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs
  - api-core/tests/Diax.Tests/Customers/WebsiteClassifierTests.cs
  - api-core/tests/Diax.Tests/Customers/JunkDomainFilterTests.cs
autonomous: true
requirements: [EXTR-01, EXTR-03]

must_haves:
  truths:
    - "Uma URL de diretório de terceiro (econodata, facebook, linktr.ee...) é classificada como Directory"
    - "Uma URL de site próprio (clinicaxyz.com.br) é classificada como OwnSite"
    - "Uma URL vazia, '-', 'n/a' ou malformada é classificada como Unknown, sem lançar exceção"
    - "Um domínio placeholder/lixo (instagram.local, wixpress.com, sentry.io, example.com) é reconhecido como lixo sem nenhuma query DNS"
  artifacts:
    - path: "api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs"
      provides: "enum WebsiteKind { Unknown = 0, OwnSite = 1, Directory = 2 }"
      contains: "enum WebsiteKind"
    - path: "api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs"
      provides: "Porta 1:1 de classify_url/DIRECTORY_HOSTS do site_check.py"
      contains: "DirectoryHosts"
      min_lines: 40
    - path: "api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs"
      provides: "Porta 1:1 de is_junk_domain do mx_check.py"
      contains: "JunkParts"
      min_lines: 40
    - path: "api-core/tests/Diax.Tests/Customers/WebsiteClassifierTests.cs"
      provides: "Tabela-verdade espelhada do site_check.py"
    - path: "api-core/tests/Diax.Tests/Customers/JunkDomainFilterTests.cs"
      provides: "Tabela-verdade espelhada do mx_check.py"
  key_links:
    - from: "api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs"
      to: "Diax.Domain.Customers.Enums.WebsiteKind"
      via: "using + tipo de retorno"
      pattern: "WebsiteKind Classify"
---

<objective>
Portar para C# as duas funções puras que hoje só existem na ponte Python: `classify_url` /
`DIRECTORY_HOSTS` (`site_check.py`) e `is_junk_domain` (`mx_check.py`). Ambas são funções puras
(sem rede, sem DB), então esta plano roda em paralelo com tudo mais na Wave 1 e não depende de
nada.

Purpose: EXTR-03 precisa da classificação site-próprio-vs-diretório; EXTR-01 precisa do
pré-filtro de domínio-lixo (o bounce de 7,5% da semana de 03-04/09 incluiu `instagram.local` e
`wixpress.com` — domínios que uma checagem de MX pura NÃO pegaria, porque `wixpress.com` TEM MX
válido). Sem esse pré-filtro, a fase não resolve o incidente que a motivou.

Output: `WebsiteKind` (enum de domínio), `WebsiteClassifier` (estático, puro),
`JunkDomainFilter` (estático, puro), com testes espelhando a tabela-verdade do Python.
</objective>

<decision_log>
**DECISÃO DO PLANNER (questão em aberto #1 da pesquisa): `is_junk_domain` ENTRA nesta fase.**

Racional: (a) `canonical_refs` do CONTEXT.md lista `is_junk_domain` explicitamente como "lógica a
ser portada"; (b) é função pura, zero I/O, custo de implementação trivial; (c) é o único filtro
que pega `wixpress.com`/`sentry.io` — domínios reais com MX válido que vazam como remetente
default de formulário e que a checagem de MX de D-01 deixaria passar; (d) o incidente concreto
citado no CONTEXT.md (`instagram.local`) é exatamente um caso de `JUNK_SUFFIXES`.

**Bucket de contagem:** um lead rejeitado por domínio-lixo conta como **"e-mail lixo"**
(`LowQualityEmailRejectedCount`), NÃO como "sem MX". Isso preserva intactos os 4 buckets da
decisão D-04 (geo / e-mail lixo / sem MX / duplicado) — nenhum bucket novo é criado.
</decision_log>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Convenções do projeto que este plano deve replicar. Não explorar o codebase — está tudo aqui. -->

Enums de domínio vivem em `api-core/src/Diax.Domain/Customers/Enums/`, namespace
`Diax.Domain.Customers.Enums`. Exemplo existente (`LeadQuality.cs`, `PersonType.cs`): enum
simples com valores explícitos e comentário XML por membro.

Projeto de testes: `api-core/tests/Diax.Tests/`, namespace `Diax.Tests.Customers`.
⚠️ **Usar `Assert.*` do xUnit puro. FluentAssertions NÃO está referenciado em
`Diax.Tests.csproj`** — `Should()` não compila. Os testes existentes usam
`Assert.True/Assert.Equal` (ver `ExtractorIntegrationServiceTests.cs` linhas 113-115).

`Diax.Application` referencia `Diax.Domain` (ProjectReference já existe) — nenhuma mudança de
csproj é necessária neste plano.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebsiteKind enum + WebsiteClassifier (porta 1:1 de classify_url)</name>

  <files>
    api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs (novo)
    api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs (novo)
    api-core/tests/Diax.Tests/Customers/WebsiteClassifierTests.cs (novo)
  </files>

  <read_first>
    - docs/email-marketing/site_check.py — linhas 24-29 (`DIRECTORY_HOSTS`) e linhas 75-85
      (`classify_url`). É a tabela-verdade; ler para confirmar a semântica antes de portar.
    - api-core/src/Diax.Domain/Customers/Enums/LeadQuality.cs — formato exato de um enum de
      domínio neste projeto (namespace, comentário XML).
    - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs — linhas 1-20
      (usings) e 108-120 (estilo de assert: `Assert.True`, `Assert.Equal`, sem FluentAssertions).
  </read_first>

  <behavior>
    - `Classify(null)` → `WebsiteKind.Unknown`
    - `Classify("")` → `Unknown`
    - `Classify("   ")` → `Unknown`
    - `Classify("-")` → `Unknown`
    - `Classify("n/a")` → `Unknown`
    - `Classify("null")` → `Unknown`
    - `Classify("none")` → `Unknown`
    - `Classify("http://")` → `Unknown` (não lança — paridade com `urlparse`, que devolve netloc vazio)
    - `Classify(":::")` → `Unknown` (não lança)
    - `Classify("localhost")` → `Unknown` (host sem '.')
    - `Classify("https://www.econodata.com.br/empresa/123")` → `Directory`
    - `Classify("facebook.com/minhaclinica")` → `Directory` (sem esquema — prefixar "http://")
    - `Classify("https://linktr.ee/fulano")` → `Directory`
    - `Classify("https://instagram.com/loja")` → `Directory`
    - `Classify("HTTPS://WWW.CLINIGUIA.COM.BR/x")` → `Directory` (case-insensitive)
    - `Classify("https://clinicaodontovix.com.br")` → `OwnSite`
    - `Classify("www.padariadobairro.com.br")` → `OwnSite`
    - `Classify("https://minhaloja.nuvemshop.com.br")` → `Directory` ("nuvemshop" está na lista)
  </behavior>

  <action>
Criar `api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs` com EXATAMENTE:

```csharp
namespace Diax.Domain.Customers.Enums;

/// <summary>
/// Classificação do campo Website de um lead: site próprio do negócio vs página de
/// diretório de terceiro (econodata, redes sociais, agregadores). Porta de
/// `classify_url` em docs/email-marketing/site_check.py.
/// </summary>
public enum WebsiteKind
{
    /// <summary>Sem website, valor placeholder ('-', 'n/a', 'null', 'none') ou URL malformada.</summary>
    Unknown = 0,

    /// <summary>Domínio próprio do negócio — sinal positivo de maturidade digital.</summary>
    OwnSite = 1,

    /// <summary>Página em diretório/agregador/rede social de terceiro — não é site próprio.</summary>
    Directory = 2
}
```

Criar `api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs` com
EXATAMENTE (a lista `DirectoryHosts` é cópia VERBATIM de `DIRECTORY_HOSTS` de
`site_check.py` linhas 24-29 — decisão D-08: não reinventar a lista, não adicionar, não
remover, não reordenar):

```csharp
using Diax.Domain.Customers.Enums;

namespace Diax.Application.Customers.WebsiteClassification;

/// <summary>
/// Classifica o `website` de um lead como site próprio vs diretório de terceiro (EXTR-03).
/// Porta 1:1 de `classify_url` / `DIRECTORY_HOSTS` em docs/email-marketing/site_check.py
/// (decisão D-08 do 07-CONTEXT.md — a lista é canônica, não reinventar).
/// Função pura: sem rede, sem I/O, sem estado. Por isso não tem interface nem é injetada.
/// </summary>
public static class WebsiteClassifier
{
    /// <summary>
    /// Páginas de terceiros que o Extrator às vezes grava como "website" do lead.
    /// Cópia verbatim de DIRECTORY_HOSTS (site_check.py, linhas 24-29).
    /// </summary>
    private static readonly string[] DirectoryHosts =
    {
        "econodata", "cliniguia", "facebook.", "instagram.", "linkedin.", "google.", "goo.gl",
        "apontador", "guiamais", "telelistas", "solutudo", "cnpj.biz", "cnpj.info", "consultacnpj",
        "empresascnpj", "casadosdados", "yelp.", "tripadvisor", "ifood", "doctoralia", "boaconsulta",
        "jusbrasil", "olx.", "mercadolivre", "shopee", "wa.me", "whatsapp.", "bit.ly", "linktr.ee",
        "youtube.", "tiktok.", "kekanto", "hotmart", "lojaintegrada", "wixsite", "site123",
        "negocio.site", "business.site", "nuvemshop", "blogspot", "wordpress.com"
    };

    /// <summary>
    /// Valores placeholder que o Extrator grava quando não achou website.
    /// Espelha `if not u or u in ('-', 'n/a', 'null', 'none')` do Python.
    /// </summary>
    private static readonly HashSet<string> PlaceholderValues =
        new(StringComparer.Ordinal) { "-", "n/a", "null", "none" };

    /// <summary>
    /// Classifica a URL. NUNCA lança — entrada malformada vira <see cref="WebsiteKind.Unknown"/>.
    /// </summary>
    public static WebsiteKind Classify(string? url)
    {
        var u = (url ?? string.Empty).Trim().ToLowerInvariant();

        if (string.IsNullOrEmpty(u) || PlaceholderValues.Contains(u))
            return WebsiteKind.Unknown;

        string host;
        try
        {
            // Espelha urlparse(u if '://' in u else 'http://' + u).netloc
            var absolute = u.Contains("://", StringComparison.Ordinal) ? u : "http://" + u;
            host = new Uri(absolute, UriKind.Absolute).Host;
        }
        catch (UriFormatException)
        {
            // Python nunca lança aqui (urlparse devolve netloc vazio) — manter paridade.
            return WebsiteKind.Unknown;
        }

        if (string.IsNullOrEmpty(host) || !host.Contains('.'))
            return WebsiteKind.Unknown;

        return DirectoryHosts.Any(d => host.Contains(d, StringComparison.Ordinal))
            ? WebsiteKind.Directory
            : WebsiteKind.OwnSite;
    }
}
```

Criar `api-core/tests/Diax.Tests/Customers/WebsiteClassifierTests.cs` com um
`[Theory]`/`[InlineData]` cobrindo TODOS os casos listados em `<behavior>` acima (um InlineData
por linha), no formato:

```csharp
using Diax.Application.Customers.WebsiteClassification;
using Diax.Domain.Customers.Enums;
using Xunit;

namespace Diax.Tests.Customers;

public class WebsiteClassifierTests
{
    [Theory]
    [InlineData(null, WebsiteKind.Unknown)]
    [InlineData("", WebsiteKind.Unknown)]
    // ... um InlineData por caso de <behavior>
    public void Classify_MatchesPythonTruthTable(string? url, WebsiteKind expected)
        => Assert.Equal(expected, WebsiteClassifier.Classify(url));
}
```

Usar `Assert.Equal` do xUnit. NÃO usar FluentAssertions (`Should()` não compila — o pacote não
está em `Diax.Tests.csproj`).
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~WebsiteClassifierTests"</automated>
  </verify>

  <acceptance_criteria>
    - `api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs` existe e contém as strings
      `Unknown = 0`, `OwnSite = 1` e `Directory = 2`
    - `grep -c "econodata\|wordpress.com" api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs` retorna ≥ 2
    - `grep -o '"[a-z0-9.\-]*"' api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs | wc -l` — o array `DirectoryHosts` tem exatamente 40 entradas (mesmo count de `DIRECTORY_HOSTS` no Python)
    - `WebsiteClassifier.cs` contém `catch (UriFormatException)` (paridade com Python: nunca lança)
    - `WebsiteClassifierTests.cs` NÃO contém a string `Should()` nem `FluentAssertions`
    - Comando de verify sai com exit code 0 e reporta ≥ 18 testes passando no filtro
  </acceptance_criteria>

  <done>
    `WebsiteClassifier.Classify` devolve `Directory` para os 40 hosts da lista canônica,
    `OwnSite` para domínio próprio, `Unknown` para vazio/placeholder/malformado, e nunca lança.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JunkDomainFilter (porta 1:1 de is_junk_domain)</name>

  <files>
    api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs (novo)
    api-core/tests/Diax.Tests/Customers/JunkDomainFilterTests.cs (novo)
  </files>

  <read_first>
    - docs/email-marketing/mx_check.py — linhas 1-28: docstring (contexto do bounce de 7,5%),
      `JUNK_SUFFIXES`, `JUNK_HOSTS`, `JUNK_PARTS` e `is_junk_domain`. Tabela-verdade.
    - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs — método privado
      `IsLowQualityEmail` (por volta da linha 190): é onde este filtro será plugado no plano
      07-04; ler agora para escrever um filtro com assinatura compatível (recebe **domínio**,
      não e-mail inteiro).
    - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs — linhas 1-20
      (usings/estilo de teste).
  </read_first>

  <behavior>
    - `IsJunk(null)` → `true`
    - `IsJunk("")` → `true`
    - `IsJunk("   ")` → `true` (trim antes)
    - `IsJunk("semponto")` → `true` (sem '.')
    - `IsJunk("localhost")` → `true` (está em JunkHosts)
    - `IsJunk("google_maps")` → `true` (está em JunkHosts)
    - `IsJunk("instagram.local")` → `true` (sufixo `.local` — caso real do bounce de 04/09)
    - `IsJunk("algo.localhost")` → `true`
    - `IsJunk("x.invalid")` / `IsJunk("x.test")` / `IsJunk("x.example")` / `IsJunk("x.internal")` / `IsJunk("x.lan")` → `true`
    - `IsJunk("example.com")` → `true`
    - `IsJunk("wixpress.com")` → `true` (caso real do bounce de 04/09)
    - `IsJunk("sentry-next.wixpress.com")` → `true`
    - `IsJunk("sentry.io")` → `true`
    - `IsJunk("gmail.con")` → `true` (typo conhecido em JunkHosts)
    - `IsJunk("google.com")` → `true` (está em JunkHosts)
    - `IsJunk("wordpress.com")` / `IsJunk("wix.com")` / `IsJunk("squarespace.com")` / `IsJunk("godaddy.com")` / `IsJunk("hostgator.com.br")` / `IsJunk("mailinator.com")` → `true`
    - `IsJunk("noemail.com.br")` / `IsJunk("sememail.org")` / `IsJunk("nomail.net")` / `IsJunk("placeholder.io")` → `true` (JunkParts, substring)
    - `IsJunk("WIXPRESS.COM")` → `true` (case-insensitive)
    - `IsJunk("clinicaodontovix.com.br")` → `false`
    - `IsJunk("gmail.com")` → `false` (webmail comercial NÃO é lixo — v2/EMAL-04 trata isso, não esta fase)
    - `IsJunk("padariadobairro.com.br")` → `false`
  </behavior>

  <action>
Criar `api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs` com EXATAMENTE
(as três listas são cópia VERBATIM de `mx_check.py` linhas 14-19 — não adicionar, não remover):

```csharp
namespace Diax.Application.Customers.Services;

/// <summary>
/// Detecta domínios de e-mail placeholder / lixo ANTES de qualquer query DNS (custo zero de I/O).
/// Porta 1:1 de `is_junk_domain` em docs/email-marketing/mx_check.py.
///
/// Por que existe além da checagem de MX: `wixpress.com` e `sentry.io` são domínios REAIS com MX
/// válido — infraestrutura de terceiros que vaza como remetente default de formulário. Uma
/// checagem de MX pura NÃO pegaria esses casos. O bounce de 7,5% da semana de 03-04/09 incluiu
/// `instagram.local` e `wixpress.com`.
///
/// Um lead rejeitado por aqui conta no bucket "e-mail lixo" (LowQualityEmailRejectedCount),
/// não no bucket "sem MX" — preserva os 4 buckets da decisão D-04.
///
/// Função pura: sem rede, sem I/O, sem estado.
/// </summary>
public static class JunkDomainFilter
{
    /// <summary>Cópia verbatim de JUNK_SUFFIXES (mx_check.py linha 14).</summary>
    private static readonly string[] JunkSuffixes =
    {
        ".local", ".localhost", ".invalid", ".test", ".example", ".internal", ".lan"
    };

    /// <summary>Cópia verbatim de JUNK_HOSTS (mx_check.py linhas 15-18).</summary>
    private static readonly HashSet<string> JunkHosts = new(StringComparer.Ordinal)
    {
        "localhost", "example.com", "example.com.br", "email.com", "site.com.br", "site.com",
        "test.com", "teste.com", "teste.com.br", "dominio.com", "dominio.com.br",
        "seusite.com.br", "empresa.com.br", "google_maps", "google.com", "gmail.con",
        "hotmail.con", "wixpress.com", "sentry.io", "sentry-next.wixpress.com", "wordpress.com",
        "wix.com", "squarespace.com", "godaddy.com", "hostgator.com.br", "mailinator.com"
    };

    /// <summary>Cópia verbatim de JUNK_PARTS (mx_check.py linha 19).</summary>
    private static readonly string[] JunkParts =
    {
        "wixpress", "sentry", "placeholder", "noemail", "sememail", "nomail"
    };

    /// <summary>
    /// True se o domínio é comprovadamente inútil para entrega de e-mail.
    /// Recebe o DOMÍNIO (parte depois do '@'), não o e-mail inteiro.
    /// </summary>
    public static bool IsJunk(string? domain)
    {
        var d = (domain ?? string.Empty).Trim().ToLowerInvariant();

        if (string.IsNullOrEmpty(d))
            return true;

        // Espelha `if not d or '.' not in d and d != 'localhost' and d != 'google_maps'`.
        // (localhost e google_maps também estão em JunkHosts, então o resultado final é o mesmo,
        //  mas a estrutura é mantida idêntica ao Python para facilitar auditoria de paridade.)
        if (!d.Contains('.') && d != "localhost" && d != "google_maps")
            return true;

        if (JunkHosts.Contains(d))
            return true;

        if (JunkSuffixes.Any(s => d.EndsWith(s, StringComparison.Ordinal)))
            return true;

        return JunkParts.Any(p => d.Contains(p, StringComparison.Ordinal));
    }

    /// <summary>
    /// Conveniência: extrai o domínio de um e-mail e aplica <see cref="IsJunk(string?)"/>.
    /// E-mail nulo/vazio ou sem '@' devolve false (não é papel deste filtro rejeitar malformado —
    /// a validação de e-mail a jusante já faz isso com erro rastreável).
    /// </summary>
    public static bool IsJunkEmail(string? email)
    {
        if (string.IsNullOrWhiteSpace(email))
            return false;

        var at = email.LastIndexOf('@');
        if (at < 0 || at == email.Length - 1)
            return false;

        return IsJunk(email[(at + 1)..]);
    }
}
```

Criar `api-core/tests/Diax.Tests/Customers/JunkDomainFilterTests.cs`:
- Um `[Theory]` com um `[InlineData]` para CADA caso de `<behavior>` acima, chamando
  `JunkDomainFilter.IsJunk`.
- Mais 3 `[Fact]` para `IsJunkEmail`:
  - `IsJunkEmail("contato@wixpress.com")` → `true`
  - `IsJunkEmail("contato@clinicaodontovix.com.br")` → `false`
  - `IsJunkEmail("semarroba")` → `false` (malformado não é papel deste filtro)
- `Assert.Equal` / `Assert.True` / `Assert.False` do xUnit. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~JunkDomainFilterTests"</automated>
  </verify>

  <acceptance_criteria>
    - `api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs` existe e contém as
      strings `JunkSuffixes`, `JunkHosts`, `JunkParts`, `IsJunk`, `IsJunkEmail`
    - `grep -c "wixpress" api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs` retorna ≥ 2 (aparece em JunkHosts e em JunkParts)
    - `grep -c "instagram" api-core/src/Diax.Application/Customers/Services/JunkDomainFilter.cs` retorna 0 — `instagram.local` é pego pelo sufixo `.local`, NÃO por entrada dedicada (prova que a porta é fiel)
    - `JunkDomainFilterTests.cs` NÃO contém `Should()` nem `FluentAssertions`
    - Comando de verify sai com exit code 0 e reporta ≥ 25 testes passando
  </acceptance_criteria>

  <done>
    `JunkDomainFilter.IsJunk` reproduz a tabela-verdade de `is_junk_domain` para os 26 JunkHosts,
    7 JunkSuffixes e 6 JunkParts, e devolve `false` para domínios comerciais legítimos.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build --filter "FullyQualifiedName~WebsiteClassifierTests|FullyQualifiedName~JunkDomainFilterTests"
dotnet test -c Release --no-build   # suíte completa: baseline 707 + os novos, tudo verde
```
</verification>

<success_criteria>
- `WebsiteKind`, `WebsiteClassifier` e `JunkDomainFilter` existem e compilam em Release
- Testes novos passam; suíte completa continua verde (≥ 707 testes, 0 falhas)
- Nenhum arquivo fora de `files_modified` foi tocado
- Nenhuma dependência nova de NuGet foi adicionada neste plano
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-01-SUMMARY.md`
</output>
