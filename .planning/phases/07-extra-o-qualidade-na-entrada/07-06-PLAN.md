---
phase: 07-extra-o-qualidade-na-entrada
plan: 06
type: execute
wave: 5
depends_on: ["07-05"]
files_modified:
  - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs
  - api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs
  - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs
  - api-core/src/Diax.Application/Customers/CustomerImportService.cs
  - api-core/src/Diax.Api/Controllers/V1/CustomersController.cs
  - api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs
autonomous: true
requirements: [EXTR-02]

must_haves:
  truths:
    - "É possível consultar, por período, quantos leads foram descartados e por qual motivo"
    - "GET /api/v1/customers/imports?from=...&to=... devolve só as rodadas do período pedido"
    - "A resposta de cada rodada expõe os 4 contadores de rejeição"
    - "Chamar o endpoint sem from/to mantém exatamente o comportamento atual (não quebra o front)"
  artifacts:
    - path: "api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs"
      provides: "GetPagedAsync com filtro opcional de período"
      contains: "DateTime? from"
    - path: "api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs"
      provides: "ImportHistoryResponse com os 4 contadores"
      contains: "NoMxRejectedCount"
    - path: "api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs"
      provides: "Cobertura do filtro de período e do mapeamento dos contadores"
  key_links:
    - from: "api-core/src/Diax.Api/Controllers/V1/CustomersController.cs"
      to: "GetImportHistoryAsync(page, pageSize, from, to)"
      via: "query params [FromQuery] DateTime? from / to"
      pattern: "DateTime\\? from"
    - from: "api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs"
      to: "customer_imports.created_at"
      via: "Where(x => x.CreatedAt >= from && x.CreatedAt <= to)"
      pattern: "CreatedAt >="
---

<objective>
Fechar a segunda metade de EXTR-02: os motivos de rejeição já são gravados (plano 07-05), agora
precisam ser **consultáveis por período**. Expor os 4 contadores em `ImportHistoryResponse` e
adicionar filtro `from`/`to` ao `GET /api/v1/customers/imports`, que já existe e já ordena por
`CreatedAt` com índice (`IX_CustomerImports_CreatedAt`).

Purpose: o texto de EXTR-01..03 no REQUIREMENTS.md diz "consultável por período" — sem esse
endpoint, os contadores existiriam só no banco, sem superfície de consulta, e o requisito não
estaria fechado.

Output: endpoint filtrável + DTO com os contadores. Sem frontend (o CRM-web não é escopo desta
fase; a consulta é feita via API/Swagger).
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contratos exatos. Não explorar o codebase. -->

Estado ATUAL de `api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs`:
```csharp
public interface ICustomerImportRepository : IRepository<CustomerImport>
{
    Task<(IEnumerable<CustomerImport> Items, int TotalCount)> GetPagedAsync(
        int page, int pageSize, CancellationToken cancellationToken = default);
}
```

Estado ATUAL de `api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs`
(arquivo inteiro, 35 linhas):
```csharp
public class CustomerImportRepository : Repository<CustomerImport>, ICustomerImportRepository
{
    public CustomerImportRepository(DiaxDbContext context) : base(context) { }

    public async Task<(IEnumerable<CustomerImport> Items, int TotalCount)> GetPagedAsync(
        int page, int pageSize, CancellationToken cancellationToken = default)
    {
        var query = DbSet.OrderByDescending(x => x.CreatedAt);
        var totalCount = await query.CountAsync(cancellationToken);
        var items = await query.Skip((page - 1) * pageSize).Take(pageSize).ToListAsync(cancellationToken);
        return (items, totalCount);
    }
}
```

Estado ATUAL de `ImportHistoryResponse` em
`api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs`:
```csharp
public record ImportHistoryResponse(
    Guid Id, string FileName, string Type, string Status, int TotalRecords,
    int SuccessCount, int FailedCount, string? ErrorDetails, DateTime CreatedAt)
{
    public static ImportHistoryResponse FromEntity(CustomerImport import) => new(
        import.Id, import.FileName, import.Type.ToString(), import.Status.ToString(),
        import.TotalRecords, import.SuccessCount, import.FailedCount,
        import.ErrorDetails, import.CreatedAt);
}
```

Estado ATUAL de `CustomerImportService.GetImportHistoryAsync` (~linha 708):
```csharp
public async Task<PagedResponse<ImportHistoryResponse>> GetImportHistoryAsync(
    int page, int pageSize, CancellationToken cancellationToken = default)
{
    var (items, totalCount) = await _importRepository.GetPagedAsync(page, pageSize, cancellationToken);
    var responses = items.Select(ImportHistoryResponse.FromEntity);
    return PagedResponse<ImportHistoryResponse>.Create(responses, page, pageSize, totalCount);
}
```

Estado ATUAL do endpoint em `api-core/src/Diax.Api/Controllers/V1/CustomersController.cs` (~linha 382):
```csharp
[HttpGet("imports")]
[ProducesResponseType(typeof(PagedResponse<ImportHistoryResponse>), StatusCodes.Status200OK)]
public async Task<IActionResult> GetImportHistory(
    [FromQuery] int page = 1, [FromQuery] int pageSize = 20,
    CancellationToken cancellationToken = default)
{
    _logger.LogInformation("Buscando histórico de importações - Page: {Page}, PageSize: {PageSize}", page, pageSize);
    var result = await _importService.GetImportHistoryAsync(page, pageSize, cancellationToken);
    return Ok(result);
}
```

De 07-03, `CustomerImport` tem `GeoRejectedCount`, `LowQualityEmailRejectedCount`,
`NoMxRejectedCount`, `DuplicateRejectedCount` (todos `int`, `private set`).

⚠️ Convenção de DB: `DateTime` é convertido para UTC no save/read via ValueConverter no
`DiaxDbContext`. Os filtros `from`/`to` devem ser tratados como UTC.
⚠️ Testes: `Assert.*` do xUnit puro. **FluentAssertions NÃO está em `Diax.Tests.csproj`.**
`Microsoft.EntityFrameworkCore.InMemory` 8.0.11 ESTÁ disponível — usar para testar o repositório.
⚠️ Build/test SEMPRE com `-c Release`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filtro de período no repositório + ImportHistoryResponse com os 4 contadores</name>

  <files>
    api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs
    api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs
    api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs
    api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs (novo)
  </files>

  <read_first>
    - api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs — arquivo inteiro.
    - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs — arquivo inteiro.
    - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs — o record
      `ImportHistoryResponse` e seu `FromEntity`.
    - api-core/src/Diax.Domain/Customers/CustomerImport.cs — os 4 contadores criados no plano 07-03.
    - Qualquer teste existente em `api-core/tests/Diax.Tests/` que use
      `new DbContextOptionsBuilder<DiaxDbContext>().UseInMemoryDatabase(...)` — copiar o padrão de
      construção do `DiaxDbContext` em teste (ele tem dependências de interceptor/CurrentUserService;
      se o padrão não existir no repo, testar o filtro via `IQueryable` em memória em vez de
      instanciar o DbContext).
  </read_first>

  <behavior>
    `GetPagedAsync(page, pageSize, from, to, ct)`:
    - `from == null && to == null` → devolve tudo (comportamento idêntico ao atual)
    - `from = 2026-09-01` com rodadas em 31/08 e 02/09 → devolve só a de 02/09
    - `to = 2026-09-01` com rodadas em 31/08 e 02/09 → devolve só a de 31/08
    - `from` e `to` no mesmo dia → devolve as rodadas daquele intervalo, inclusive nas bordas
    - `TotalCount` reflete o total FILTRADO, não o total da tabela
    - Ordenação continua `OrderByDescending(CreatedAt)`

    `ImportHistoryResponse.FromEntity`:
    - Mapeia os 4 contadores da entidade para as 4 propriedades novas do record
    - Mantém todas as 9 propriedades atuais, na mesma ordem posicional
  </behavior>

  <action>
1. Em `api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs`, trocar a assinatura por:
```csharp
    /// <summary>
    /// Obtém lista paginada de importações, opcionalmente restrita a um período (EXTR-02:
    /// "motivo de rejeição consultável por período"). Datas são UTC e inclusivas nas bordas.
    /// </summary>
    Task<(IEnumerable<CustomerImport> Items, int TotalCount)> GetPagedAsync(
        int page,
        int pageSize,
        DateTime? from = null,
        DateTime? to = null,
        CancellationToken cancellationToken = default);
```
   ⚠️ Parâmetros novos com default ANTES do `CancellationToken` — chamadores nomeados/posicionais
   existentes (`GetPagedAsync(page, pageSize, cancellationToken)`) passariam o CT na posição de
   `from` e NÃO compilariam. Isso é intencional: força atualizar o único chamador
   (`CustomerImportService.GetImportHistoryAsync`) na Task 2. Se o compilador reclamar em algum
   outro lugar, atualizar também — usar `grep -rn "GetPagedAsync(" --include=*.cs api-core/src`
   para achar TODOS os chamadores antes de compilar.

2. Em `api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs`,
   reescrever o método:
```csharp
    public async Task<(IEnumerable<CustomerImport> Items, int TotalCount)> GetPagedAsync(
        int page,
        int pageSize,
        DateTime? from = null,
        DateTime? to = null,
        CancellationToken cancellationToken = default)
    {
        IQueryable<CustomerImport> query = DbSet;

        // Filtro por período — usa o índice IX_CustomerImports_CreatedAt que já existe.
        if (from.HasValue)
            query = query.Where(x => x.CreatedAt >= from.Value);

        if (to.HasValue)
            query = query.Where(x => x.CreatedAt <= to.Value);

        var totalCount = await query.CountAsync(cancellationToken);

        var items = await query
            .OrderByDescending(x => x.CreatedAt)
            .Skip((page - 1) * pageSize)
            .Take(pageSize)
            .ToListAsync(cancellationToken);

        return (items, totalCount);
    }
```

3. Em `api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs`, estender
   `ImportHistoryResponse` — adicionar as 4 propriedades NO FINAL (para não alterar a ordem
   posicional das 9 existentes) e mapeá-las em `FromEntity`:
```csharp
public record ImportHistoryResponse(
    Guid Id,
    string FileName,
    string Type,
    string Status,
    int TotalRecords,
    int SuccessCount,
    int FailedCount,
    string? ErrorDetails,
    DateTime CreatedAt,
    // ===== EXTR-02: motivos de rejeição desta rodada =====
    int GeoRejectedCount = 0,
    int LowQualityEmailRejectedCount = 0,
    int NoMxRejectedCount = 0,
    int DuplicateRejectedCount = 0)
{
    public static ImportHistoryResponse FromEntity(CustomerImport import)
    {
        return new ImportHistoryResponse(
            import.Id,
            import.FileName,
            import.Type.ToString(),
            import.Status.ToString(),
            import.TotalRecords,
            import.SuccessCount,
            import.FailedCount,
            import.ErrorDetails,
            import.CreatedAt,
            import.GeoRejectedCount,
            import.LowQualityEmailRejectedCount,
            import.NoMxRejectedCount,
            import.DuplicateRejectedCount);
    }
}
```

4. Criar `api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs`:
   - 4 `[Fact]` para o mapeamento de `FromEntity` (construir um `CustomerImport`, chamar
     `RecordRejectionCounts(3, 2, 1, 7)` e `Complete(...)`, assertar que os 4 contadores chegam ao
     DTO e que as 9 propriedades originais continuam corretas).
   - 5 `[Fact]` para o filtro de período. Preferir `DiaxDbContext` com
     `UseInMemoryDatabase(Guid.NewGuid().ToString())` seguindo o padrão de construção já usado em
     outro teste do repo. Se instanciar o `DiaxDbContext` exigir dependências demais (interceptors,
     `ICurrentUserService`), **fallback aceitável**: extrair a expressão de filtro para um método
     estático testável e testar sobre `IQueryable` em memória
     (`new List<CustomerImport>{...}.AsQueryable()`), documentando a escolha no SUMMARY.
   - `Assert.*` do xUnit puro. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~ImportHistoryQueryTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "DateTime? from" api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs` retorna 1
    - `grep -c "x.CreatedAt >= from.Value" api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs` retorna 1
    - `grep -c "x.CreatedAt <= to.Value" api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs` retorna 1
    - `api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs` contém as 4 strings
      `GeoRejectedCount`, `LowQualityEmailRejectedCount`, `NoMxRejectedCount`, `DuplicateRejectedCount`
    - `grep -c "import.NoMxRejectedCount" api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs` retorna 1
    - Comando de verify sai com exit code 0 e reporta ≥ 9 testes passando
  </acceptance_criteria>

  <done>
    O repositório filtra por período usando o índice existente e o DTO de histórico carrega os 4
    contadores de rejeição.
  </done>
</task>

<task type="auto">
  <name>Task 2: Query params from/to no GET /api/v1/customers/imports</name>

  <files>
    api-core/src/Diax.Application/Customers/CustomerImportService.cs
    api-core/src/Diax.Api/Controllers/V1/CustomersController.cs
  </files>

  <read_first>
    - api-core/src/Diax.Application/Customers/CustomerImportService.cs — método
      `GetImportHistoryAsync` (~linha 708) e o campo `_importRepository`.
    - api-core/src/Diax.Api/Controllers/V1/CustomersController.cs — o método `GetImportHistory`
      (~linha 382) e outros métodos do MESMO controller que já recebem filtro de data por
      `[FromQuery]`, para copiar a convenção de nome/formato (procurar por `[FromQuery] DateTime`).
    - Saída de `grep -rn "GetImportHistoryAsync\|GetPagedAsync(" --include=*.cs api-core/src` —
      confirmar que TODOS os chamadores foram atualizados antes de compilar.
  </read_first>

  <action>
1. Em `CustomerImportService.GetImportHistoryAsync`, trocar por:
```csharp
    public async Task<PagedResponse<ImportHistoryResponse>> GetImportHistoryAsync(
        int page,
        int pageSize,
        DateTime? from = null,
        DateTime? to = null,
        CancellationToken cancellationToken = default)
    {
        var (items, totalCount) = await _importRepository.GetPagedAsync(
            page,
            pageSize,
            from,
            to,
            cancellationToken);

        var responses = items.Select(ImportHistoryResponse.FromEntity);

        return PagedResponse<ImportHistoryResponse>.Create(
            responses,
            page,
            pageSize,
            totalCount);
    }
```

2. Em `CustomersController.GetImportHistory`, trocar por:
```csharp
    /// <summary>
    /// Obtém o histórico de importações, com os motivos de rejeição por rodada (EXTR-02).
    /// </summary>
    /// <param name="page">Número da página (padrão: 1)</param>
    /// <param name="pageSize">Tamanho da página (padrão: 20)</param>
    /// <param name="from">Data inicial (UTC, inclusiva). Opcional.</param>
    /// <param name="to">Data final (UTC, inclusiva). Opcional.</param>
    /// <param name="cancellationToken">Token de cancelamento</param>
    [HttpGet("imports")]
    [ProducesResponseType(typeof(PagedResponse<ImportHistoryResponse>), StatusCodes.Status200OK)]
    public async Task<IActionResult> GetImportHistory(
        [FromQuery] int page = 1,
        [FromQuery] int pageSize = 20,
        [FromQuery] DateTime? from = null,
        [FromQuery] DateTime? to = null,
        CancellationToken cancellationToken = default)
    {
        _logger.LogInformation(
            "Buscando histórico de importações - Page: {Page}, PageSize: {PageSize}, From: {From}, To: {To}",
            page, pageSize, from, to);

        var result = await _importService.GetImportHistoryAsync(page, pageSize, from, to, cancellationToken);

        return Ok(result);
    }
```

3. Compilar e corrigir qualquer outro chamador que o grep tenha revelado.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "FromQuery] DateTime? from" api-core/src/Diax.Api/Controllers/V1/CustomersController.cs` retorna 1
    - `grep -c "FromQuery] DateTime? to" api-core/src/Diax.Api/Controllers/V1/CustomersController.cs` retorna 1
    - `grep -c "GetImportHistoryAsync(page, pageSize, from, to, cancellationToken)" api-core/src/Diax.Api/Controllers/V1/CustomersController.cs` retorna 1
    - `dotnet build -c Release` sai com exit code 0 e **0 warnings novos**
    - `dotnet test -c Release --no-build` (suíte COMPLETA) sai com exit code 0, 0 falhas
  </acceptance_criteria>

  <done>
    `GET /api/v1/customers/imports?from=2026-09-01&to=2026-09-30` devolve as rodadas do período
    com os 4 contadores de rejeição; chamar sem `from`/`to` mantém o comportamento anterior.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build          # suíte completa, 0 falhas

# Smoke local (opcional, exige API rodando):
cd src/Diax.Api && dotnet run &
curl -s "http://localhost:5062/api/v1/customers/imports?page=1&pageSize=5" -H "Authorization: Bearer $TOKEN" | head -40
```
</verification>

<success_criteria>
- EXTR-02 fechado: o motivo de rejeição de cada rodada é gravado (07-05) E consultável por
  período via API (este plano)
- Chamar `GET /api/v1/customers/imports` sem `from`/`to` produz exatamente a resposta de antes,
  acrescida dos 4 campos novos (compatível com o frontend atual, que ignora campos extras)
- Suíte completa verde, 0 falhas, 0 warnings novos de build
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-06-SUMMARY.md`
</output>
