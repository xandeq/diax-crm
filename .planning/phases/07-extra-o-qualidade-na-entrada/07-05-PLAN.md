---
phase: 07-extra-o-qualidade-na-entrada
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs
  - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
  - api-core/src/Diax.Application/Customers/CustomerImportService.cs
  - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs
  - api-core/tests/Diax.Tests/Customers/CustomerImportServiceTests.cs
autonomous: true
requirements: [EXTR-02, EXTR-03]

must_haves:
  truths:
    - "Ao final de uma rodada de import do Extrator, o CustomerImport daquela rodada carrega os 4 contadores de rejeição preenchidos"
    - "O contador de duplicados reflete quantos leads casaram com um Customer já existente"
    - "O website de um lead do Extrator chega ao Customer (hoje ele só vira texto em Notes e é perdido)"
    - "Um Customer criado a partir de um lead com website de diretório nasce com WebsiteKind = Directory"
    - "Um Customer existente que ganha website no enriquecimento tem WebsiteKind recalculado"
  artifacts:
    - path: "api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs"
      provides: "record ImportRejectionCounts + campo opcional em BulkImportRequest"
      contains: "ImportRejectionCounts"
    - path: "api-core/src/Diax.Application/Customers/CustomerImportService.cs"
      provides: "Persistência dos contadores + classificação de website no create/update"
      contains: "RecordRejectionCounts"
  key_links:
    - from: "api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs"
      to: "BulkImportRequest.RejectionCounts"
      via: "preenchimento dos contadores locais na chamada de ImportAsync"
      pattern: "RejectionCounts:"
    - from: "api-core/src/Diax.Application/Customers/CustomerImportService.cs"
      to: "CustomerImport.RecordRejectionCounts"
      via: "chamada antes de import.Complete()"
      pattern: "import.RecordRejectionCounts"
    - from: "api-core/src/Diax.Application/Customers/CustomerImportService.cs"
      to: "WebsiteClassifier.Classify"
      via: "customer.SetWebsiteKind(...) no create e no enriquecimento"
      pattern: "SetWebsiteKind"
---

<objective>
Fazer os contadores de rejeição atravessarem a fronteira Application (EXTR-02) e a classificação
de site chegar ao banco (EXTR-03).

Purpose: hoje `ExtractorIntegrationService` computa `skippedByGeo`/`rejectedLowQuality` como
variáveis locais que só viram `LogInformation` — nunca chegam ao `CustomerImport`. E o `website`
do lead do Extrator é jogado dentro de `Notes` como texto (`$"Website: {lead.Website}"`), nunca
chegando ao campo `Customer.Website`. Ambos são os gaps exatos que EXTR-02 e EXTR-03 fecham.

Output: `ImportRejectionCounts` no `BulkImportRequest`, `CustomerImport` gravando os 4
contadores, `Customer.Website` + `Customer.WebsiteKind` preenchidos no import.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contratos exatos disponíveis quando este plano roda. Não explorar o codebase. -->

De 07-01:
```csharp
namespace Diax.Application.Customers.WebsiteClassification;
public static class WebsiteClassifier { public static WebsiteKind Classify(string? url); }
namespace Diax.Domain.Customers.Enums;
public enum WebsiteKind { Unknown = 0, OwnSite = 1, Directory = 2 }
```

De 07-03:
```csharp
// Diax.Domain.Customers.Customer
public WebsiteKind WebsiteKind { get; private set; }
public string? ExternalId { get; private set; }
public void SetWebsiteKind(WebsiteKind kind);
public void SetExternalId(string? externalId);
public void UpdateContactInfo(string? phone = null, string? whatsApp = null,
                              string? secondaryEmail = null, string? website = null);

// Diax.Domain.Customers.CustomerImport
public int GeoRejectedCount { get; private set; }
public int LowQualityEmailRejectedCount { get; private set; }
public int NoMxRejectedCount { get; private set; }
public int DuplicateRejectedCount { get; private set; }
public void RecordRejectionCounts(int geo, int lowQualityEmail, int noMx, int duplicate);
public void Complete(int successCount, int failedCount, string? errors);
```

Estado ATUAL de `api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs`:
```csharp
public record ImportCustomerRow(
    string Name, string Email, string? Phone = null, string? WhatsApp = null,
    string? CompanyName = null, string? Notes = null, string? Tags = null,
    string? Website = null, string? City = null, string? CurrentTool = null,
    string? MainPain = null, string? ValidationStatus = null, string? ConsentStatus = null);

public record BulkImportRequest(
    List<ImportCustomerRow> Customers,
    LeadSource Source = LeadSource.Import,
    bool DryRun = false);
```
⚠️ `ImportCustomerRow` JÁ TEM `Website` — mas `ExtractorIntegrationService.MapToImportRow` NÃO o
preenche: ele monta `noteParts.Add($"Website: {lead.Website}")` e passa `Website` implicitamente
como `null`. Esse é o bug silencioso que EXTR-03 expõe.

Pontos exatos em `api-core/src/Diax.Application/Customers/CustomerImportService.cs`:
- linha ~315: `var import = new CustomerImport(fileName, ImportType.CSV, request.Customers.Count);`
- linha ~318-322: declaração de `errors`, `successCount`, `skippedCount`
- linha ~467-476: ramo CREATE — `var customer = new Customer(...)` seguido de
  `customer.UpdateContactInfo(sanitized.Phone, sanitized.WhatsApp, website: row.Website);`
- linha ~544: início do ramo `else` (existingCustomer != null) — "Se EXISTE, ENRIQUECER OS DADOS"
- linha ~566-580: bloco "Atualiza Telefones e Website se vazio", com `newWebsite` e
  `existingCustomer.UpdateContactInfo(newPhone, newWhatsApp, existingCustomer.SecondaryEmail, newWebsite)`
- linha ~682: `import.Complete(successCount, errors.Count, errorJson);`
- linha ~685: `await _unitOfWork.SaveChangesAsync(cancellationToken);`

Estado ATUAL de `ExtractorIntegrationService.ImportLeadsAsync` (após o plano 07-04): contadores
locais `skippedContactless`, `rejectedLowQuality`, `skippedByGeo`, `rejectedNoMx`, `mxUnverified`;
chamada final:
```csharp
var importRequest = new BulkImportRequest(Customers: allLeads, Source: LeadSource.Scraping);
var fileName = $"Extrator de Dados - {DateTime.UtcNow:yyyy-MM-dd HH:mm}";
var importResult = await _customerImportService.ImportAsync(importRequest, fileName, cancellationToken);
```

⚠️ Testes: `Assert.*` do xUnit puro. **FluentAssertions NÃO está em `Diax.Tests.csproj`.**
⚠️ Build/test SEMPRE com `-c Release`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ImportRejectionCounts no BulkImportRequest + Extractor preenche contadores e Website</name>

  <files>
    api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs
    api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
    api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs
  </files>

  <read_first>
    - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs — LER INTEIRO. Note que
      `ImportCustomerRow` já tem `Website` como 8º parâmetro posicional opcional.
    - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs — LER INTEIRO, com
      atenção especial a `MapToImportRow` (final do arquivo) e ao comentário longo de
      IDEMPOTÊNCIA que explica por que `lead.Id` hoje só vai para `Notes`.
    - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs — helper `MakeLead`
      (para saber se ele aceita website; se não, estender).
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md — linhas 224-253 (Pattern 3:
      por que colunas e não JSON, e como os contadores atravessam a fronteira).
  </read_first>

  <behavior>
    - `new BulkImportRequest(rows, LeadSource.Import)` (sem contadores) → `RejectionCounts == null`
      — nenhum chamador existente quebra
    - Após um import do Extrator com 3 leads fora do ES, 2 com e-mail lixo e 1 sem MX, o
      `BulkImportRequest` passado a `ImportAsync` tem
      `RejectionCounts == new ImportRejectionCounts(3, 2, 1)`
    - `MapToImportRow` de um lead com `Website = "https://econodata.com.br/x"` produz um
      `ImportCustomerRow` com `Website == "https://econodata.com.br/x"`
    - `MapToImportRow` continua colocando `"Website: ..."` em `Notes` (não remover — é
      rastreabilidade humana já usada em produção)
    - `MapToImportRow` de um lead sem website → `Website == null`
  </behavior>

  <action>
1. Em `api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs`, ANTES do record
   `BulkImportRequest`, adicionar:

```csharp
/// <summary>
/// Contadores agregados de leads descartados ANTES do import, computados pelo chamador
/// (hoje: ExtractorIntegrationService). Persistidos em CustomerImport (EXTR-02 / decisão D-04).
/// O contador de duplicados NÃO vem daqui — só o CustomerImportService sabe quantas linhas
/// casaram com um Customer existente, então ele o computa por conta própria.
/// </summary>
public record ImportRejectionCounts(
    int GeoRejected = 0,
    int LowQualityEmailRejected = 0,
    int NoMxRejected = 0);
```
   E alterar `BulkImportRequest` para:
```csharp
public record BulkImportRequest(
    List<ImportCustomerRow> Customers,
    LeadSource Source = LeadSource.Import,
    bool DryRun = false,
    ImportRejectionCounts? RejectionCounts = null);   // opcional: só o Extrator preenche
```
   ⚠️ Parâmetro NOVO no FINAL e com default — nenhum chamador posicional existente quebra.

2. Em `api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs`:

   a) Trocar a construção do request por:
```csharp
        var importRequest = new BulkImportRequest(
            Customers: allLeads,
            Source: LeadSource.Scraping,
            RejectionCounts: new ImportRejectionCounts(
                GeoRejected: skippedByGeo,
                LowQualityEmailRejected: rejectedLowQuality,
                NoMxRejected: rejectedNoMx)
        );
```

   b) Em `MapToImportRow`, no `return new ImportCustomerRow(...)`, adicionar o argumento nomeado
      `Website: lead.Website`:
```csharp
        return new ImportCustomerRow(
            Name: name,
            Email: lead.Email ?? string.Empty,
            Phone: lead.Phone,
            WhatsApp: lead.WhatsApp ?? lead.Phone,
            CompanyName: lead.CompanyName,
            Notes: noteParts.Count > 0 ? string.Join("\n", noteParts) : null,
            Tags: string.Join(",", tags),
            Website: lead.Website        // ← EXTR-03: o website precisa chegar ao Customer,
                                         //   não só ao texto de Notes
        );
```
      **NÃO remover** o `noteParts.Add($"Website: {lead.Website}")` — a nota é rastreabilidade
      humana já usada em produção.

3. Em `ExtractorIntegrationServiceTests.cs`:
   a) Se `MakeLead` não aceitar website, adicionar um parâmetro opcional
      `string? website = null` que preenche `ExtractorLead.Website`.
   b) Adicionar um `[Fact]` por caso de `<behavior>` (5 fatos). Para capturar o
      `BulkImportRequest` real, usar um callback no mock do repositório ou assertar via o
      `CustomerImport` capturado em `_importRepoMock` — o mais direto é capturar o
      `CustomerImport` no `AddAsync` já mockado no fixture:
```csharp
        CustomerImport? captured = null;
        _importRepoMock
            .Setup(r => r.AddAsync(It.IsAny<CustomerImport>(), It.IsAny<CancellationToken>()))
            .ReturnsAsync((CustomerImport ci, CancellationToken _) => { captured = ci; return ci; });
```
      (a asserção sobre os contadores GRAVADOS é da Task 2; aqui basta assertar que
      `MapToImportRow` propaga o Website e que `ImportRejectionCounts` é montado com os valores
      certos — para isso, capturar o `BulkImportRequest` exige um mock de
      `CustomerImportService`, que hoje é uma CLASSE CONCRETA no fixture. Se for inviável mockar,
      assertar indiretamente pelo `CustomerImport` capturado depois de a Task 2 estar pronta, e
      neste momento cobrir apenas os 3 casos de `MapToImportRow`/`Website`).
   `Assert.*` do xUnit puro. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~ExtractorIntegrationServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "record ImportRejectionCounts" api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs` retorna 1
    - `grep -c "ImportRejectionCounts? RejectionCounts = null" api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs` retorna 1
    - `grep -c "Website: lead.Website" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna 1
    - `grep -c "Website: {lead.Website}" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna 1 (a nota NÃO foi removida)
    - `grep -c "RejectionCounts: new ImportRejectionCounts" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna 1
    - `dotnet test -c Release --no-build` (suíte COMPLETA) sai com exit code 0, 0 falhas
  </acceptance_criteria>

  <done>
    O `BulkImportRequest` do Extrator carrega os 3 contadores pré-computados, e o `website` do
    lead chega ao `ImportCustomerRow` em vez de se perder dentro de `Notes`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CustomerImportService persiste os 4 contadores e classifica o website</name>

  <files>
    api-core/src/Diax.Application/Customers/CustomerImportService.cs
    api-core/tests/Diax.Tests/Customers/CustomerImportServiceTests.cs
  </files>

  <read_first>
    - api-core/src/Diax.Application/Customers/CustomerImportService.cs — LER as linhas 300-330
      (criação do `CustomerImport` e declaração dos contadores locais), 460-500 (ramo CREATE),
      540-600 (ramo ENRIQUECER, com o bloco de website) e 655-700 (bloco final: `import.Complete`,
      `SaveChangesAsync`, `return new BulkImportResponse(...)`).
    - api-core/tests/Diax.Tests/Customers/CustomerImportServiceTests.cs — LER INTEIRO, para
      reaproveitar o fixture de mocks existente em vez de criar outro.
    - api-core/src/Diax.Domain/Customers/CustomerImport.cs — método `RecordRejectionCounts`
      criado no plano 07-03.
    - api-core/src/Diax.Application/Customers/WebsiteClassification/WebsiteClassifier.cs — criado
      no plano 07-01.
  </read_first>

  <behavior>
    - Import com `RejectionCounts = new ImportRejectionCounts(3, 2, 1)` e 0 duplicados →
      o `CustomerImport` gravado tem `GeoRejectedCount == 3`, `LowQualityEmailRejectedCount == 2`,
      `NoMxRejectedCount == 1`, `DuplicateRejectedCount == 0`
    - Import com `RejectionCounts = null` → os 4 contadores ficam em 0 (nenhuma exceção)
    - Import de 3 linhas em que 2 casam com `Customer` existente →
      `DuplicateRejectedCount == 2` (conta tanto o caminho "enriqueceu" quanto o
      "duplicata ignorada, nada novo")
    - `RecordRejectionCounts` é chamado ANTES de `import.Complete(...)` e os valores sobrevivem
      ao `Complete`
    - Criar Customer com `row.Website = "https://econodata.com.br/x"` →
      `customer.WebsiteKind == WebsiteKind.Directory` e `customer.Website` preenchido
    - Criar Customer com `row.Website = "https://clinicaodonto.com.br"` →
      `WebsiteKind == WebsiteKind.OwnSite`
    - Criar Customer com `row.Website = null` → `WebsiteKind == WebsiteKind.Unknown`
    - Enriquecer Customer existente SEM website que recebe `row.Website = "linktr.ee/x"` →
      `WebsiteKind == WebsiteKind.Directory`
    - Enriquecer Customer existente que JÁ tem website → o website existente é preservado e
      `WebsiteKind` é recalculado a partir do website FINAL (o preservado)
  </behavior>

  <action>
1. Em `api-core/src/Diax.Application/Customers/CustomerImportService.cs`:

   a) Adicionar no topo: `using Diax.Application.Customers.WebsiteClassification;`

   b) Junto de `var successCount = 0; var skippedCount = 0;` (~linha 320), adicionar:
```csharp
        var duplicateCount = 0;   // linhas que casaram com um Customer já existente (EXTR-02)
```

   c) No ramo CREATE, LOGO DEPOIS de
      `customer.UpdateContactInfo(sanitized.Phone, sanitized.WhatsApp, website: row.Website);`
      adicionar:
```csharp
                    // EXTR-03: site próprio vs diretório de terceiro, calculado no import.
                    customer.SetWebsiteKind(WebsiteClassifier.Classify(row.Website));
```

   d) No ramo ENRIQUECER (`else`, existingCustomer != null), como PRIMEIRA linha do bloco:
```csharp
                    duplicateCount++;
```
      E, logo DEPOIS do bloco
      `if (contactUpdated || newWebsite != existingCustomer.Website) { existingCustomer.UpdateContactInfo(...); wasUpdated = true; }`,
      adicionar:
```csharp
                    // EXTR-03: recalcula sempre a partir do website FINAL (o preservado ou o novo),
                    // para que leads legados importados antes desta fase também sejam classificados.
                    var recomputedKind = WebsiteClassifier.Classify(existingCustomer.Website);
                    if (recomputedKind != existingCustomer.WebsiteKind)
                    {
                        existingCustomer.SetWebsiteKind(recomputedKind);
                        wasUpdated = true;
                    }
```

   e) Logo ANTES de `import.Complete(successCount, errors.Count, errorJson);` (~linha 682),
      adicionar:
```csharp
        // EXTR-02 / D-04: contadores agregados por rodada. Geo/e-mail-lixo/sem-MX vêm
        // pré-computados do chamador (ExtractorIntegrationService, que filtra ANTES de chegar
        // aqui); duplicados só este service sabe contar.
        var rejectionCounts = request.RejectionCounts ?? new ImportRejectionCounts();
        import.RecordRejectionCounts(
            geo: rejectionCounts.GeoRejected,
            lowQualityEmail: rejectionCounts.LowQualityEmailRejected,
            noMx: rejectionCounts.NoMxRejected,
            duplicate: duplicateCount);
```
      ⚠️ Existe um `return` ANTECIPADO no caminho `DryRun`/validação (linhas ~305-310) que sai
      antes de o `CustomerImport` ser criado — NÃO adicionar `RecordRejectionCounts` lá; aquele
      caminho não persiste nenhum `CustomerImport`.

2. Em `api-core/tests/Diax.Tests/Customers/CustomerImportServiceTests.cs`, adicionar um `[Fact]`
   por caso de `<behavior>` (9 fatos). Padrão: capturar o `CustomerImport` no mock de
   `ICustomerImportRepository.AddAsync` e o `Customer` no mock de `ICustomerRepository.AddAsync`:
```csharp
        CustomerImport? capturedImport = null;
        importRepoMock.Setup(r => r.AddAsync(It.IsAny<CustomerImport>(), It.IsAny<CancellationToken>()))
            .ReturnsAsync((CustomerImport ci, CancellationToken _) => { capturedImport = ci; return ci; });

        Customer? capturedCustomer = null;
        customerRepoMock.Setup(r => r.AddAsync(It.IsAny<Customer>(), It.IsAny<CancellationToken>()))
            .ReturnsAsync((Customer c, CancellationToken _) => { capturedCustomer = c; return c; });
```
   Para o caso de duplicata, configurar
   `customerRepoMock.Setup(r => r.GetByEmailAsync(email, ...)).ReturnsAsync(existingCustomer)`.
   `Assert.*` do xUnit puro. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~CustomerImportServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "import.RecordRejectionCounts" api-core/src/Diax.Application/Customers/CustomerImportService.cs` retorna 1
    - `grep -c "SetWebsiteKind" api-core/src/Diax.Application/Customers/CustomerImportService.cs` retorna 2 (create + enriquecimento)
    - `grep -c "duplicateCount++" api-core/src/Diax.Application/Customers/CustomerImportService.cs` retorna 1
    - `grep -n "RecordRejectionCounts" api-core/src/Diax.Application/Customers/CustomerImportService.cs`
      retorna um número de linha MENOR que o de `grep -n "import.Complete("` (contadores antes do Complete)
    - `grep -c "using Diax.Application.Customers.WebsiteClassification" api-core/src/Diax.Application/Customers/CustomerImportService.cs` retorna 1
    - `dotnet test -c Release --no-build` (suíte COMPLETA) sai com exit code 0, 0 falhas
  </acceptance_criteria>

  <done>
    O `CustomerImport` de cada rodada carrega os 4 motivos de rejeição, e todo `Customer`
    criado/enriquecido pelo import nasce com `WebsiteKind` calculado.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build --filter "FullyQualifiedName~CustomerImportServiceTests"
dotnet test -c Release --no-build --filter "FullyQualifiedName~ExtractorIntegrationServiceTests"
dotnet test -c Release --no-build          # suíte completa, 0 falhas
```
</verification>

<success_criteria>
- EXTR-02 (metade da persistência): os 4 contadores por motivo chegam ao `CustomerImport` de cada rodada
- EXTR-03 (persistência): `Customer.Website` e `Customer.WebsiteKind` preenchidos no create e no enriquecimento
- Nenhum chamador existente de `BulkImportRequest` quebrou (parâmetro novo é opcional, no final)
- A nota `"Website: ..."` em `Notes` continua sendo gerada (rastreabilidade humana preservada)
- Suíte completa verde, 0 falhas
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-05-SUMMARY.md`
</output>
