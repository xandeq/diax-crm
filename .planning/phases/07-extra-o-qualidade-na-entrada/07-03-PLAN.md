---
phase: 07-extra-o-qualidade-na-entrada
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - api-core/src/Diax.Domain/Customers/Customer.cs
  - api-core/src/Diax.Domain/Customers/CustomerImport.cs
  - api-core/src/Diax.Domain/Customers/MxCacheEntry.cs
  - api-core/src/Diax.Domain/Customers/IMxCacheRepository.cs
  - api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs
  - api-core/src/Diax.Infrastructure/Data/Configurations/CustomerImportConfiguration.cs
  - api-core/src/Diax.Infrastructure/Data/Configurations/MxCacheEntryConfiguration.cs
  - api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs
  - api-core/src/Diax.Infrastructure/Data/Repositories/MxCacheRepository.cs
  - api-core/src/Diax.Infrastructure/DependencyInjection.cs
  - api-core/tests/Diax.Tests/Customers/CustomerImportCountersTests.cs
  - api-core/tests/Diax.Tests/Customers/MxCacheEntryTests.cs
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03]

must_haves:
  truths:
    - "Customer tem uma propriedade WebsiteKind que persiste a classificação do site"
    - "Customer tem uma propriedade ExternalId (consumida pela Phase 8) com índice único filtrado"
    - "CustomerImport carrega 4 contadores de rejeição por rodada (geo / e-mail lixo / sem MX / duplicado)"
    - "Existe uma tabela de cache de MX por domínio, com TTL avaliado em código, que sobrevive à reciclagem do App Pool"
  artifacts:
    - path: "api-core/src/Diax.Domain/Customers/MxCacheEntry.cs"
      provides: "Entidade de cache persistente de MX por domínio (D-03)"
      contains: "class MxCacheEntry"
    - path: "api-core/src/Diax.Domain/Customers/IMxCacheRepository.cs"
      provides: "GetByDomainAsync / UpsertAsync"
      contains: "GetByDomainAsync"
    - path: "api-core/src/Diax.Infrastructure/Data/Configurations/MxCacheEntryConfiguration.cs"
      provides: "Tabela mx_cache_entries + índice único em domain"
      contains: "IX_MxCacheEntries_Domain"
    - path: "api-core/src/Diax.Infrastructure/Data/Repositories/MxCacheRepository.cs"
      provides: "Implementação EF do cache"
  key_links:
    - from: "api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs"
      to: "MxCacheEntry"
      via: "DbSet<MxCacheEntry> MxCacheEntries"
      pattern: "DbSet<MxCacheEntry>"
    - from: "api-core/src/Diax.Infrastructure/DependencyInjection.cs"
      to: "IMxCacheRepository"
      via: "AddScoped<IMxCacheRepository, MxCacheRepository>()"
      pattern: "IMxCacheRepository, MxCacheRepository"
    - from: "api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs"
      to: "customers.external_id"
      via: "HasIndex único filtrado por IS NOT NULL"
      pattern: "IX_Customers_ExternalId"
---

<objective>
Colocar no modelo de domínio + no mapeamento EF tudo que a fase precisa persistir:
`Customer.WebsiteKind` (EXTR-03), `Customer.ExternalId` (D-07 — criado AQUI, consumido na
Phase 8), os 4 contadores de rejeição em `CustomerImport` (EXTR-02, D-04) e a tabela de cache de
MX por domínio (EXTR-01, D-03).

Purpose: um único ponto de coordenação de schema para toda a fase. A migration em si é gerada no
plano 07-07 — este plano deixa o modelo pronto e testado, sem tocar em `Migrations/`.

Output: entidades, repositório de cache, configurations EF, DbSet e registros de DI. Zero
mudança de comportamento em runtime ainda (nada consome os campos novos até 07-04/07-06).
</objective>

<decision_log>
**Cache de MX = tabela EF, não IMemoryCache, não arquivo (discretion do planner sobre D-03).**
- `IMemoryCache` é em processo — some a cada reciclagem do App Pool do IIS, que em shared hosting
  acontece com frequência (idle timeout, deploy FTP, memory pressure). Um cache de 30 dias que
  reseta toda hora não cumpre D-03 ("evita ~1000 queries DNS/dia").
- Arquivo (espelhando `mx-cache.json`) não tem precedente no projeto para dado durável — a única
  escrita em `App_Data` hoje é `startup-error.txt`, artefato de diagnóstico — e seria sobrescrito
  no próximo deploy FTP.
- Tabela EF é o único mecanismo durável já estabelecido. Volume: domínios ÚNICOS vistos pelo
  Extrator (milhares), não linhas por lead. Comparar com `audit_logs` (279 MB): esta tabela nunca
  chega perto disso, então não agrava a quota de 467/500 MB que motivou D-05.

**Contadores = 4 colunas `int`, não JSON em `ErrorDetails` (discretion do planner sobre D-04).**
`ErrorDetails` já tem propósito estabelecido: lista POR LINHA de `ImportError(RowNumber, Email,
ErrorMessage)`. Misturar agregados ali obrigaria a parsear JSON para responder "quantos
rejeitados por sem-MX este mês". 4 colunas `int` respondem isso com SQL direto e espelham o
padrão que `SuccessCount`/`FailedCount` já estabelecem na MESMA entidade. Consultável por
período via o índice `IX_CustomerImports_CreatedAt` que já existe.

**`ExternalId` entra AQUI (D-07), mesmo sendo consumido só na Phase 8.** O 07-CONTEXT.md é
explícito: uma migration única para os dois campos, porque existe pendência aberta de ordenação
de migrations (v1.2 `20260529134701_AddAgentFoundation` aplicada em produção com commits não
pushados) e um ponto de coordenação é melhor que dois.
</decision_log>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Contratos exatos do codebase. Não explorar. -->

De `api-core/src/Diax.Domain/Common/AuditableEntity.cs`:
```csharp
public abstract class AuditableEntity : Entity
{
    public DateTime CreatedAt { get; protected set; }
    public string? CreatedBy { get; protected set; }
    public DateTime? UpdatedAt { get; protected set; }
    public string? UpdatedBy { get; protected set; }
    protected AuditableEntity() : base() { CreatedAt = DateTime.UtcNow; }
    public void SetUpdated(string? userId = null) { ... }
}
```

De `api-core/src/Diax.Domain/Common/IRepository.cs`:
```csharp
public interface IRepository<T> where T : Entity
{
    Task<T?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<IEnumerable<T>> FindAsync(Expression<Func<T, bool>> predicate, CancellationToken ct = default);
    Task<T> AddAsync(T entity, CancellationToken ct = default);
    Task UpdateAsync(T entity, CancellationToken ct = default);
    // ... GetAllAsync, GetPagedAsync, DeleteAsync, ExistsAsync, CountAsync
}
```
Implementação base: `api-core/src/Diax.Infrastructure/Data/Repositories/Repository<T>` — expõe
`DbSet` protegido e recebe `DiaxDbContext` no construtor (ver `CustomerImportRepository.cs`,
16 linhas, é o modelo exato a copiar).

De `api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs`:
- Convenção automática: PascalCase → snake_case nos nomes de coluna.
- `DateTime` convertido para UTC no save/read via ValueConverter.
- Strings sem `HasMaxLength` viram 256 por default.
- DbSets de Customers estão nas linhas 70-72:
  `public DbSet<Customer> Customers => Set<Customer>();`
  `public DbSet<CustomerImport> CustomerImports => Set<CustomerImport>();`

De `api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs`:
- `builder.ToTable("customers")`, enums mapeados com `.HasConversion<int>()`
- Padrão de índice único filtrado já usado no arquivo (linha ~130):
  `builder.HasIndex(c => c.Document).IsUnique().HasFilter("[document] IS NOT NULL");`

De `api-core/src/Diax.Infrastructure/Data/Configurations/CustomerImportConfiguration.cs`:
- `builder.ToTable("customer_imports")`, índices `IX_CustomerImports_CreatedAt` e `IX_CustomerImports_Status`.

De `api-core/src/Diax.Infrastructure/DependencyInjection.cs` linha ~201:
`services.AddScoped<ICustomerImportRepository, CustomerImportRepository>();`

Vindo dos planos da Wave 1 (JÁ EXISTEM quando este plano roda):
- `Diax.Domain.Customers.Enums.WebsiteKind` { Unknown = 0, OwnSite = 1, Directory = 2 }  [07-01]
- `Diax.Application.Customers.Services.MxCheckResult` { Valid = 0, NoMx = 1, Unverified = 2 }  [07-02]

⚠️ `MxCacheEntry` fica em `Diax.Domain` mas precisa do tipo `MxCheckResult`, que está em
`Diax.Application`. **Domain NÃO pode referenciar Application.** Solução obrigatória: guardar o
resultado como `int` na entidade de domínio (`public int ResultCode`), com a conversão para
`MxCheckResult` feita na camada Application (plano 07-04). Ver a action da Task 2.

⚠️ Testes: `Assert.*` do xUnit puro. **FluentAssertions NÃO está em `Diax.Tests.csproj`.**
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Customer.WebsiteKind + Customer.ExternalId + contadores em CustomerImport</name>

  <files>
    api-core/src/Diax.Domain/Customers/Customer.cs
    api-core/src/Diax.Domain/Customers/CustomerImport.cs
    api-core/tests/Diax.Tests/Customers/CustomerImportCountersTests.cs (novo)
  </files>

  <read_first>
    - api-core/src/Diax.Domain/Customers/Customer.cs — LER INTEIRO. Note: todas as props têm
      `private set`; mutação só via método de domínio (`UpdateContactInfo`, `UpdateClassification`,
      `UpdateSegmentation`, ...). Não quebrar esse padrão.
    - api-core/src/Diax.Domain/Customers/CustomerImport.cs — LER INTEIRO (é curto). Note o método
      `Complete(successCount, failedCount, errors)` que define `Status`.
    - api-core/src/Diax.Domain/Customers/Enums/WebsiteKind.cs — criado no plano 07-01.
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md — decisões D-04, D-06, D-07.
  </read_first>

  <behavior>
    - `new CustomerImport("f", ImportType.CSV, 10)` → os 4 contadores começam em `0`
    - `import.RecordRejectionCounts(geo: 5, lowQualityEmail: 3, noMx: 2, duplicate: 7)` →
      `GeoRejectedCount == 5`, `LowQualityEmailRejectedCount == 3`, `NoMxRejectedCount == 2`,
      `DuplicateRejectedCount == 7`
    - `RecordRejectionCounts` com valores negativos → normaliza para 0 (nunca grava negativo)
    - `RecordRejectionCounts` NÃO altera `Status`, `SuccessCount` nem `FailedCount`
    - `Complete(...)` chamado DEPOIS de `RecordRejectionCounts` preserva os 4 contadores
    - `new Customer("Acme")` → `WebsiteKind == WebsiteKind.Unknown` e `ExternalId == null`
    - `customer.SetWebsiteKind(WebsiteKind.Directory)` → `WebsiteKind == Directory`
    - `customer.SetExternalId("12345")` → `ExternalId == "12345"`
    - `customer.SetExternalId("  ")` → `ExternalId == null` (whitespace vira null, para não
      colidir no índice único filtrado)
  </behavior>

  <action>
1. Em `api-core/src/Diax.Domain/Customers/Customer.cs`:

   a) Adicionar `using Diax.Domain.Customers.Enums;` se ainda não houver (o arquivo já usa
      `LeadSource`, então provavelmente já tem).

   b) Logo APÓS a propriedade `Website` (linha ~60), adicionar:
```csharp
    /// <summary>
    /// Classificação do Website: site próprio vs página de diretório de terceiro (EXTR-03).
    /// Calculada no import por WebsiteClassifier. Default Unknown para todo lead legado.
    /// </summary>
    public WebsiteKind WebsiteKind { get; private set; } = WebsiteKind.Unknown;
```

   c) Logo APÓS a propriedade `SourceDetails` (linha ~72), adicionar:
```csharp
    /// <summary>
    /// ID durável do lead na fonte externa (hoje: `lead.Id` do Extrator de Dados).
    /// Chave de dedup cross-pull para leads que mudam de e-mail entre passadas do scraper.
    /// Criada nesta fase por decisão D-07 (migration única com WebsiteKind); a lógica de dedup
    /// que a consome é a Phase 8 (IMPT-01). Null para leads sem origem externa.
    /// </summary>
    public string? ExternalId { get; private set; }
```

   d) Na seção de métodos de domínio, logo depois de `UpdateContactInfo`, adicionar:
```csharp
    /// <summary>
    /// Define a classificação do website (EXTR-03). Método separado de UpdateContactInfo para
    /// não quebrar as ~15 chamadas existentes daquele método.
    /// </summary>
    public void SetWebsiteKind(WebsiteKind kind)
    {
        WebsiteKind = kind;
    }

    /// <summary>
    /// Define o ID externo (Extrator). Whitespace vira null para não colidir no índice
    /// único filtrado de external_id.
    /// </summary>
    public void SetExternalId(string? externalId)
    {
        ExternalId = string.IsNullOrWhiteSpace(externalId) ? null : externalId.Trim();
    }
```

2. Em `api-core/src/Diax.Domain/Customers/CustomerImport.cs`:

   a) Logo APÓS a propriedade `ErrorDetails`, adicionar:
```csharp
    // ===== CONTADORES DE REJEIÇÃO POR RODADA (EXTR-02 / decisão D-04) =====
    // Agregados, NÃO por lead: o banco está em ~467/500 MB de quota e ~900 leads/dia são
    // rejeitados — uma tabela por lead precisaria de purge desde o primeiro dia (D-05).
    // Consultável por período via o índice IX_CustomerImports_CreatedAt que já existe.

    /// <summary>Leads descartados por UF/DDD fora da região alvo.</summary>
    public int GeoRejectedCount { get; private set; }

    /// <summary>Leads descartados por e-mail lixo (domínio bloqueado, TLD estrangeiro, '%', domínio placeholder).</summary>
    public int LowQualityEmailRejectedCount { get; private set; }

    /// <summary>Leads descartados porque o domínio de e-mail comprovadamente não recebe e-mail (sem MX e sem A).</summary>
    public int NoMxRejectedCount { get; private set; }

    /// <summary>Leads que casaram com um Customer já existente (update, não create).</summary>
    public int DuplicateRejectedCount { get; private set; }
```

   b) No construtor `CustomerImport(string fileName, ImportType type, int totalRecords)`,
      inicializar explicitamente os 4 em `0` (junto de `SuccessCount = 0; FailedCount = 0;`).

   c) Na seção de métodos de domínio, ANTES de `Complete`, adicionar:
```csharp
    /// <summary>
    /// Registra os contadores agregados de rejeição desta rodada de import (EXTR-02).
    /// Não altera Status/SuccessCount/FailedCount — pode ser chamado antes ou depois de Complete.
    /// Valores negativos são normalizados para 0.
    /// </summary>
    public void RecordRejectionCounts(int geo, int lowQualityEmail, int noMx, int duplicate)
    {
        GeoRejectedCount = Math.Max(0, geo);
        LowQualityEmailRejectedCount = Math.Max(0, lowQualityEmail);
        NoMxRejectedCount = Math.Max(0, noMx);
        DuplicateRejectedCount = Math.Max(0, duplicate);
    }
```

3. Criar `api-core/tests/Diax.Tests/Customers/CustomerImportCountersTests.cs` com um `[Fact]`
   por caso de `<behavior>` acima (9 fatos). Namespace `Diax.Tests.Customers`, `Assert.*` do
   xUnit puro, sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~CustomerImportCountersTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "WebsiteKind" api-core/src/Diax.Domain/Customers/Customer.cs` retorna ≥ 4
    - `grep -c "ExternalId" api-core/src/Diax.Domain/Customers/Customer.cs` retorna ≥ 3
    - `api-core/src/Diax.Domain/Customers/Customer.cs` contém `public void SetWebsiteKind(WebsiteKind kind)` e `public void SetExternalId(string? externalId)`
    - `api-core/src/Diax.Domain/Customers/CustomerImport.cs` contém as 4 strings
      `GeoRejectedCount`, `LowQualityEmailRejectedCount`, `NoMxRejectedCount`, `DuplicateRejectedCount`
      e `public void RecordRejectionCounts(int geo, int lowQualityEmail, int noMx, int duplicate)`
    - Nenhuma propriedade nova tem `set` público: `grep -c "public int Geo.*{ get; set; }" api-core/src/Diax.Domain/Customers/CustomerImport.cs` retorna 0
    - Comando de verify sai com exit code 0 e reporta ≥ 9 testes passando
  </acceptance_criteria>

  <done>
    `Customer` expõe `WebsiteKind`/`ExternalId` com setters de domínio; `CustomerImport` carrega
    os 4 contadores de rejeição com um método de domínio que os normaliza; testes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Entidade MxCacheEntry + IMxCacheRepository + MxCacheRepository + DI</name>

  <files>
    api-core/src/Diax.Domain/Customers/MxCacheEntry.cs (novo)
    api-core/src/Diax.Domain/Customers/IMxCacheRepository.cs (novo)
    api-core/src/Diax.Infrastructure/Data/Repositories/MxCacheRepository.cs (novo)
    api-core/src/Diax.Infrastructure/DependencyInjection.cs
    api-core/tests/Diax.Tests/Customers/MxCacheEntryTests.cs (novo)
  </files>

  <read_first>
    - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerImportRepository.cs — arquivo
      inteiro (16 linhas). É o molde exato: herda `Repository<T>`, recebe `DiaxDbContext`, usa
      `DbSet` protegido.
    - api-core/src/Diax.Domain/Customers/ICustomerImportRepository.cs — molde da interface de
      repositório específico (herda `IRepository<T>`, adiciona só o que é específico).
    - api-core/src/Diax.Domain/Common/AuditableEntity.cs — arquivo inteiro.
    - api-core/src/Diax.Infrastructure/DependencyInjection.cs — linhas ~195-210, para inserir o
      registro no mesmo bloco dos outros repositórios.
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md — linhas 192-222 (Pattern 2).
  </read_first>

  <behavior>
    - `new MxCacheEntry("acme.com.br", 0)` → `Domain == "acme.com.br"`, `ResultCode == 0`,
      `CheckedAt` ≈ `DateTime.UtcNow`
    - `new MxCacheEntry("  ACME.COM.BR ", 1)` → `Domain == "acme.com.br"` (trim + lowercase)
    - `entry.Refresh(2)` → `ResultCode == 2` e `CheckedAt` atualizado
    - `entry.IsFresh(DateTime.UtcNow, validFor: TimeSpan.FromDays(30))` → `true` para entrada recém-criada
    - Entrada com `CheckedAt` de 31 dias atrás → `IsFresh(now, TimeSpan.FromDays(30))` → `false`
    - Entrada com `CheckedAt` de 25 horas atrás → `IsFresh(now, TimeSpan.FromHours(24))` → `false`
  </behavior>

  <action>
1. Criar `api-core/src/Diax.Domain/Customers/MxCacheEntry.cs`:

```csharp
using Diax.Domain.Common;

namespace Diax.Domain.Customers;

/// <summary>
/// Cache persistente do resultado da checagem de MX por DOMÍNIO (decisão D-03), espelhando o
/// `previews/mx-cache.json` (30 dias) da ponte Python.
///
/// Por que tabela e não IMemoryCache: o App Pool do IIS em shared hosting recicla com frequência
/// (idle timeout, deploy FTP, memory pressure) — um cache em processo de 30 dias não sobrevive.
/// Volume: domínios ÚNICOS vistos pelo Extrator (milhares), não linhas por lead.
///
/// ResultCode é `int` (não o enum MxCheckResult) porque MxCheckResult vive em Diax.Application
/// e Diax.Domain NÃO referencia Application. A conversão é feita na camada Application.
/// Valores: 0 = Valid, 1 = NoMx, 2 = Unverified (espelha MxCheckResult).
/// </summary>
public class MxCacheEntry : AuditableEntity
{
    /// <summary>Domínio normalizado (lowercase, sem espaço, sem '@').</summary>
    public string Domain { get; private set; } = string.Empty;

    /// <summary>Espelha MxCheckResult: 0 = Valid, 1 = NoMx, 2 = Unverified.</summary>
    public int ResultCode { get; private set; }

    /// <summary>Momento (UTC) da última resolução real por DNS.</summary>
    public DateTime CheckedAt { get; private set; }

    /// <summary>Construtor para EF Core.</summary>
    protected MxCacheEntry() { }

    public MxCacheEntry(string domain, int resultCode)
    {
        Domain = Normalize(domain);
        ResultCode = resultCode;
        CheckedAt = DateTime.UtcNow;
    }

    /// <summary>Regrava o resultado e reinicia o TTL.</summary>
    public void Refresh(int resultCode)
    {
        ResultCode = resultCode;
        CheckedAt = DateTime.UtcNow;
    }

    /// <summary>True se a entrada ainda está dentro da janela de validade informada.</summary>
    public bool IsFresh(DateTime nowUtc, TimeSpan validFor) => nowUtc - CheckedAt < validFor;

    /// <summary>Normalização canônica do domínio — usar SEMPRE antes de consultar o cache.</summary>
    public static string Normalize(string? domain)
        => (domain ?? string.Empty).Trim().TrimStart('@').ToLowerInvariant();
}
```

2. Criar `api-core/src/Diax.Domain/Customers/IMxCacheRepository.cs`:

```csharp
using Diax.Domain.Common;

namespace Diax.Domain.Customers;

/// <summary>Repositório do cache persistente de MX por domínio (EXTR-01 / D-03).</summary>
public interface IMxCacheRepository : IRepository<MxCacheEntry>
{
    /// <summary>Busca a entrada pelo domínio já normalizado (MxCacheEntry.Normalize). Null se não existe.</summary>
    Task<MxCacheEntry?> GetByDomainAsync(string domain, CancellationToken cancellationToken = default);

    /// <summary>Carrega de uma vez todas as entradas dos domínios informados (evita N+1 no import em lote).</summary>
    Task<IReadOnlyDictionary<string, MxCacheEntry>> GetByDomainsAsync(
        IReadOnlyCollection<string> domains,
        CancellationToken cancellationToken = default);
}
```

3. Criar `api-core/src/Diax.Infrastructure/Data/Repositories/MxCacheRepository.cs` seguindo
   EXATAMENTE o molde de `CustomerImportRepository.cs`:

```csharp
using Diax.Domain.Customers;
using Microsoft.EntityFrameworkCore;

namespace Diax.Infrastructure.Data.Repositories;

/// <summary>Implementação EF do cache persistente de MX.</summary>
public class MxCacheRepository : Repository<MxCacheEntry>, IMxCacheRepository
{
    public MxCacheRepository(DiaxDbContext context) : base(context)
    {
    }

    public async Task<MxCacheEntry?> GetByDomainAsync(string domain, CancellationToken cancellationToken = default)
    {
        var d = MxCacheEntry.Normalize(domain);
        return await DbSet.FirstOrDefaultAsync(x => x.Domain == d, cancellationToken);
    }

    public async Task<IReadOnlyDictionary<string, MxCacheEntry>> GetByDomainsAsync(
        IReadOnlyCollection<string> domains,
        CancellationToken cancellationToken = default)
    {
        var normalized = domains.Select(MxCacheEntry.Normalize)
            .Where(d => !string.IsNullOrEmpty(d))
            .Distinct()
            .ToList();

        if (normalized.Count == 0)
            return new Dictionary<string, MxCacheEntry>();

        var entries = await DbSet
            .Where(x => normalized.Contains(x.Domain))
            .ToListAsync(cancellationToken);

        return entries.ToDictionary(e => e.Domain, e => e);
    }
}
```
   ⚠️ Se `Repository<T>` expuser o DbSet com outro nome que não `DbSet`, usar o nome real —
   confirmar lendo `api-core/src/Diax.Infrastructure/Data/Repositories/Repository.cs`.

4. Em `api-core/src/Diax.Infrastructure/DependencyInjection.cs`, na mesma vizinhança de
   `services.AddScoped<ICustomerImportRepository, CustomerImportRepository>();` (~linha 201),
   adicionar:
```csharp
        services.AddScoped<IMxCacheRepository, MxCacheRepository>();
```

5. Criar `api-core/tests/Diax.Tests/Customers/MxCacheEntryTests.cs` com um `[Fact]` por caso de
   `<behavior>` (6 fatos). Para os casos de TTL, construir a entidade e forçar o `CheckedAt` via
   o próprio `Refresh` + comparação com um `nowUtc` deslocado no futuro
   (ex.: `entry.IsFresh(DateTime.UtcNow.AddDays(31), TimeSpan.FromDays(30))` → `false`) —
   NÃO usar reflection para escrever em `CheckedAt`.
   `Assert.*` do xUnit puro, sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~MxCacheEntryTests"</automated>
  </verify>

  <acceptance_criteria>
    - `api-core/src/Diax.Domain/Customers/MxCacheEntry.cs` contém `class MxCacheEntry : AuditableEntity`,
      `public static string Normalize`, `public bool IsFresh`, `public void Refresh`
    - `grep -c "MxCheckResult" api-core/src/Diax.Domain/Customers/MxCacheEntry.cs` retorna 0 fora de
      comentários — a Domain NÃO pode importar tipo da Application (o grep deve achar a palavra
      apenas dentro do bloco `///`)
    - `grep -c "using Diax.Application" api-core/src/Diax.Domain/Customers/MxCacheEntry.cs` retorna 0
    - `api-core/src/Diax.Domain/Customers/IMxCacheRepository.cs` contém `GetByDomainAsync` e `GetByDomainsAsync`
    - `grep -n "IMxCacheRepository, MxCacheRepository" api-core/src/Diax.Infrastructure/DependencyInjection.cs` acha 1 ocorrência
    - Comando de verify sai com exit code 0 e reporta ≥ 6 testes passando
  </acceptance_criteria>

  <done>
    `MxCacheEntry` + repositório existem, compilam e estão registrados no DI; a Domain continua
    sem referência à Application.
  </done>
</task>

<task type="auto">
  <name>Task 3: Configurations EF + DbSet (customers.website_kind, customers.external_id, contadores, mx_cache_entries)</name>

  <files>
    api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs
    api-core/src/Diax.Infrastructure/Data/Configurations/CustomerImportConfiguration.cs
    api-core/src/Diax.Infrastructure/Data/Configurations/MxCacheEntryConfiguration.cs (novo)
    api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs
  </files>

  <read_first>
    - api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs — LER
      INTEIRO. Note o padrão de índice único filtrado na linha ~130:
      `builder.HasIndex(c => c.Document).IsUnique().HasFilter("[document] IS NOT NULL");`
      e o padrão de enum `.HasConversion<int>()`.
    - api-core/src/Diax.Infrastructure/Data/Configurations/CustomerImportConfiguration.cs — LER
      INTEIRO (62 linhas). É o molde de estilo e o lugar onde os 4 contadores entram.
    - api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs — linhas 60-100 (bloco de DbSets) e
      o `OnModelCreating` (para conferir se as configurations são aplicadas por
      `ApplyConfigurationsFromAssembly` — se sim, nenhum registro manual é necessário).
  </read_first>

  <action>
1. Em `CustomerConfiguration.cs`:

   a) Logo depois do mapeamento de `Website` (`builder.Property(c => c.Website).HasMaxLength(500);`),
      adicionar:
```csharp
        builder.Property(c => c.WebsiteKind)
            .IsRequired()
            .HasConversion<int>()
            .HasDefaultValue(WebsiteKind.Unknown);   // leads legados nascem Unknown
```
      (`using Diax.Domain.Customers.Enums;` já existe no arquivo.)

   b) Logo depois do mapeamento de `SourceDetails`, adicionar:
```csharp
        builder.Property(c => c.ExternalId)
            .HasMaxLength(64);
```

   c) Na seção de índices do MESMO arquivo, adicionar:
```csharp
        // Dedup cross-pull por ID do Extrator (D-07; consumido pela Phase 8 / IMPT-01).
        // Índice FILTRADO: no SQL Server um índice único trata múltiplos NULL como duplicados,
        // e a esmagadora maioria dos customers legados tem external_id NULL.
        builder.HasIndex(c => c.ExternalId)
            .IsUnique()
            .HasFilter("[external_id] IS NOT NULL")
            .HasDatabaseName("IX_Customers_ExternalId");
```

2. Em `CustomerImportConfiguration.cs`, logo depois de `builder.Property(x => x.FailedCount).IsRequired();`,
   adicionar os 4 contadores com default 0 (para não quebrar as 366 linhas já existentes na tabela):
```csharp
        // Contadores de rejeição por rodada (EXTR-02 / D-04). Default 0 para as linhas legadas.
        builder.Property(x => x.GeoRejectedCount).IsRequired().HasDefaultValue(0);
        builder.Property(x => x.LowQualityEmailRejectedCount).IsRequired().HasDefaultValue(0);
        builder.Property(x => x.NoMxRejectedCount).IsRequired().HasDefaultValue(0);
        builder.Property(x => x.DuplicateRejectedCount).IsRequired().HasDefaultValue(0);
```

3. Criar `api-core/src/Diax.Infrastructure/Data/Configurations/MxCacheEntryConfiguration.cs`:

```csharp
using Diax.Domain.Customers;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace Diax.Infrastructure.Data.Configurations;

/// <summary>Configuração EF Core do cache persistente de MX por domínio (EXTR-01 / D-03).</summary>
public class MxCacheEntryConfiguration : IEntityTypeConfiguration<MxCacheEntry>
{
    public void Configure(EntityTypeBuilder<MxCacheEntry> builder)
    {
        builder.ToTable("mx_cache_entries");

        builder.HasKey(x => x.Id);

        builder.Property(x => x.Domain)
            .IsRequired()
            .HasMaxLength(255);

        builder.Property(x => x.ResultCode)
            .IsRequired();

        builder.Property(x => x.CheckedAt)
            .IsRequired();

        builder.Property(x => x.CreatedAt)
            .IsRequired();

        builder.Property(x => x.CreatedBy)
            .HasMaxLength(100);

        builder.Property(x => x.UpdatedAt);

        builder.Property(x => x.UpdatedBy)
            .HasMaxLength(100);

        // Chave lógica: um registro por domínio. TTL é avaliado em código (MxCacheEntry.IsFresh),
        // não no banco — permite TTLs diferentes por resultado (30 dias para Valid/NoMx,
        // 24h para Unverified) sem mudar o schema.
        builder.HasIndex(x => x.Domain)
            .IsUnique()
            .HasDatabaseName("IX_MxCacheEntries_Domain");

        builder.HasIndex(x => x.CheckedAt)
            .HasDatabaseName("IX_MxCacheEntries_CheckedAt");
    }
}
```

4. Em `api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs`, logo depois da linha 71
   (`public DbSet<CustomerImport> CustomerImports => Set<CustomerImport>();`), adicionar:
```csharp
    public DbSet<MxCacheEntry> MxCacheEntries => Set<MxCacheEntry>();
```
   Se `OnModelCreating` NÃO usar `ApplyConfigurationsFromAssembly`, registrar
   `new MxCacheEntryConfiguration()` explicitamente no mesmo lugar em que
   `CustomerImportConfiguration` é registrada.

⚠️ **NÃO gerar migration neste plano.** `add-migration.ps1` roda no plano 07-07, depois que todo
o modelo desta fase estiver estável. Rodar aqui produziria duas migrations onde D-07 pede uma.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "IX_Customers_ExternalId" api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs` retorna 1
    - `grep -c "HasFilter(\"\[external_id\] IS NOT NULL\")" api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs` retorna 1
    - `grep -c "WebsiteKind" api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs` retorna ≥ 2
    - `grep -c "HasDefaultValue(0)" api-core/src/Diax.Infrastructure/Data/Configurations/CustomerImportConfiguration.cs` retorna 4
    - `api-core/src/Diax.Infrastructure/Data/Configurations/MxCacheEntryConfiguration.cs` contém
      `ToTable("mx_cache_entries")` e `IX_MxCacheEntries_Domain`
    - `grep -c "DbSet<MxCacheEntry>" api-core/src/Diax.Infrastructure/Data/DiaxDbContext.cs` retorna 1
    - `ls api-core/src/Diax.Infrastructure/Data/Migrations/ | grep -ci "websitekind\|externalid\|mxcache"` retorna 0
      (prova que NENHUMA migration foi gerada neste plano)
    - `dotnet test -c Release --no-build` sai com exit code 0, ≥ 707 testes, 0 falhas
  </acceptance_criteria>

  <done>
    O modelo EF conhece `customers.website_kind`, `customers.external_id` (índice único filtrado),
    os 4 contadores em `customer_imports` e a tabela `mx_cache_entries` — tudo pronto para uma
    única migration no plano 07-07. Suíte completa verde.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build           # suíte completa: baseline 707 + novos, 0 falhas

# Sanity check: o modelo mudou mas nenhuma migration foi gerada aqui
ls src/Diax.Infrastructure/Data/Migrations/ | tail -5
git status --short api-core/src/Diax.Infrastructure/Data/Migrations/   # deve estar vazio
```
</verification>

<success_criteria>
- `Customer.WebsiteKind` (default Unknown) e `Customer.ExternalId` (nullable, índice único filtrado) mapeados
- `CustomerImport` com 4 contadores `int NOT NULL DEFAULT 0`
- `MxCacheEntry` + `IMxCacheRepository` + `MxCacheRepository` + DbSet + configuration + DI
- `Diax.Domain` continua sem referenciar `Diax.Application`
- **Zero arquivos novos em `Data/Migrations/`** — a migration é o plano 07-07
- Suíte completa verde (≥ 707 testes, 0 falhas)
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-03-SUMMARY.md`
</output>
