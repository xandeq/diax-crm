---
phase: 07-extra-o-qualidade-na-entrada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api-core/src/Diax.Application/Diax.Application.csproj
  - api-core/src/Diax.Application/Customers/Services/IMxLookupService.cs
  - api-core/src/Diax.Infrastructure/Dns/MxResponseInterpreter.cs
  - api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs
  - api-core/src/Diax.Infrastructure/DependencyInjection.cs
  - api-core/tests/Diax.Tests/Customers/MxResponseInterpreterTests.cs
autonomous: true
requirements: [EXTR-01]

must_haves:
  truths:
    - "Existe um seam (`IMxLookupService`) que permite testar o filtro de MX sem tocar a rede"
    - "Domínio com MX real resolve como Valid"
    - "Domínio NXDOMAIN resolve como NoMx"
    - "Null MX (RFC 7505, Exchange == '.') é tratado como sem MX, com fallback para registro A"
    - "Timeout de DNS resolve como Unverified — NUNCA como NoMx (D-02)"
  artifacts:
    - path: "api-core/src/Diax.Application/Customers/Services/IMxLookupService.cs"
      provides: "Contrato IMxLookupService + enum MxCheckResult (Valid/NoMx/Unverified)"
      contains: "enum MxCheckResult"
    - path: "api-core/src/Diax.Infrastructure/Dns/MxResponseInterpreter.cs"
      provides: "Função pura que decide Valid/NoMx/fallback-A a partir da resposta DNS"
      contains: "InterpretMxResponse"
    - path: "api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs"
      provides: "Implementação real com DnsClient.NET LookupClient"
      contains: "DnsResponseCode.ConnectionTimeout"
    - path: "api-core/tests/Diax.Tests/Customers/MxResponseInterpreterTests.cs"
      provides: "Cobertura determinística dos 4 cenários sem rede"
  key_links:
    - from: "api-core/src/Diax.Infrastructure/DependencyInjection.cs"
      to: "IMxLookupService"
      via: "services.AddScoped<IMxLookupService, DnsClientMxLookupService>()"
      pattern: "IMxLookupService, DnsClientMxLookupService"
    - from: "api-core/src/Diax.Infrastructure/DependencyInjection.cs"
      to: "ILookupClient"
      via: "AddSingleton com LookupClientOptions (Timeout 3s, Retries 1)"
      pattern: "AddSingleton<ILookupClient>"
---

<objective>
Criar a abstração de resolução DNS exigida pela Wave 0 do 07-VALIDATION.md: `IMxLookupService`
na camada Application (mockável via Moq) e `DnsClientMxLookupService` na Infrastructure usando
`DnsClient.NET` 1.8.0. Sem esse seam, EXTR-01 não é testável de forma determinística.

Purpose: D-01 travou `DnsClient.NET` como a lib (contra `nslookup` shell-out, frágil em shared
hosting IIS, e contra `System.Net.Dns`, que não expõe MX). D-02 exige que timeout NÃO rejeite o
lead — o que só é codificável sem ambiguidade porque `DnsClient.NET` sempre lança
`DnsResponseException(ConnectionTimeout)` para timeout, enquanto NXDOMAIN volta como resposta
normal com `HasError`.

Output: contrato + implementação + interpretador puro testável offline. NENHUM consumidor ainda
— o wiring no `ExtractorIntegrationService` é o plano 07-04.
</objective>

<decision_log>
**DECISÃO DO PLANNER (questão em aberto #2 da pesquisa): o plano NÃO assume que DNS de saída
funciona no SmarterASP.**

Não há documentação pública sobre bloqueio de UDP/53 no shared hosting do SmarterASP. A
mitigação é estrutural, não uma aposta: D-02 já garante que timeout → `Unverified` → lead passa.
No pior cenário (UDP/53 totalmente bloqueado), 100% das checagens caem em `Unverified` e a
feature simplesmente não filtra nada — o import NÃO quebra e nenhum lead é perdido. A
instrumentação que detecta esse cenário (log de warning quando a taxa de `Unverified` de uma
rodada passa de 80%) é implementada no plano 07-04, junto do loop de import.

**Decisão de testabilidade:** a decisão Valid/NoMx/fallback é extraída para uma função PURA
(`MxResponseInterpreter.InterpretMxResponse`) que recebe tipos primitivos (`IReadOnlyList<string>`
de exchanges, `bool hasError`, `int responseCode`). Isso evita ter que construir
`DnsResponseHeader`/`MxRecord`/`DnsString` do DnsClient.NET nos testes — construção de tipos de
lib de terceiro em teste é frágil e não agrega. O glue tipado (`DnsClientMxLookupService`) fica
fino o suficiente para ser validado por smoke manual.
</decision_log>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contratos e convenções que o executor precisa. Não explorar o codebase. -->

`Diax.Application.csproj` (net8.0) já lista libs utilitárias como `HtmlAgilityPack` e
`HtmlSanitizer` — `DnsClient` cabe no mesmo lugar. `Diax.Infrastructure` referencia
`Diax.Application`, então a lib fica disponível transitivamente na Infrastructure.

`Diax.Tests.csproj` (net10.0) já referencia Api/Application/Domain/Infrastructure/Shared e tem
xUnit 2.9.2 + Moq 4.20.72 + EFCore.InMemory 8.0.11.
⚠️ **FluentAssertions NÃO está no csproj** — usar `Assert.*` do xUnit puro.

API do DnsClient.NET 1.8.0 (verificada contra o código-fonte da tag de release):
```csharp
public interface ILookupClient {
    Task<IDnsQueryResponse> QueryAsync(string query, QueryType queryType,
        QueryClass queryClass = QueryClass.IN, CancellationToken cancellationToken = default);
}
public interface IDnsQueryResponse {
    IReadOnlyList<DnsResourceRecord> Answers { get; }
    bool HasError { get; }
    DnsResponseHeader Header { get; }   // Header.ResponseCode é int (RCODE)
}
// extensions: Answers.MxRecords() -> IEnumerable<MxRecord>; Answers.ARecords() -> IEnumerable<ARecord>
// MxRecord.Exchange é DnsString; DnsString.Value de um Null MX (RFC 7505) == "."
// DnsResponseCode.ConnectionTimeout == 999 (valor sintético da lib, NÃO é um RCODE real)
// DnsResponseCode.NotExistentDomain == 3 (NXDOMAIN, RCODE real)
// Defaults de LookupClientOptions: Timeout=5s, Retries=2, ThrowDnsErrors=false, UseCache=true
// Timeout SEMPRE lança DnsResponseException com Code == ConnectionTimeout, independente de
// ThrowDnsErrors. Erros de protocolo (NXDOMAIN) só lançam se ThrowDnsErrors=true.
```

Registro de DI na Infrastructure: `api-core/src/Diax.Infrastructure/DependencyInjection.cs`.
Repositórios são registrados por volta da linha 201 (`services.AddScoped<ICustomerImportRepository, CustomerImportRepository>();`)
e options por volta da linha 530 (`services.Configure<ExtractorPullOptions>(...)`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pacote DnsClient 1.8.0 + contrato IMxLookupService/MxCheckResult</name>

  <files>
    api-core/src/Diax.Application/Diax.Application.csproj
    api-core/src/Diax.Application/Customers/Services/IMxLookupService.cs (novo)
  </files>

  <read_first>
    - api-core/src/Diax.Application/Diax.Application.csproj — ver o bloco `<ItemGroup>` de
      `PackageReference` e onde `DnsClient` deve entrar (ordem alfabética: entre
      `Microsoft.AspNetCore.DataProtection.Abstractions` e ... na verdade antes de
      `FluentValidation`? conferir a ordem real do arquivo e manter consistente).
    - api-core/src/Diax.Application/Customers/Services/ExtractorService.cs — padrão de
      interface + DTO na pasta `Customers/Services` (namespace
      `Diax.Application.Customers.Services`).
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md — seção "Pattern 1",
      linhas 114-190 (semântica exata dos 3 estados).
  </read_first>

  <action>
1. Adicionar o pacote:
```bash
cd api-core
dotnet add src/Diax.Application package DnsClient --version 1.8.0
```
Conferir que `Diax.Application.csproj` ficou com `<PackageReference Include="DnsClient" Version="1.8.0" />`.
⚠️ Versão travada em **1.8.0** — é a última estável no NuGet (verificado na pesquisa); não usar
`1.8.0-beta-*` nem deixar sem `--version`.

2. Criar `api-core/src/Diax.Application/Customers/Services/IMxLookupService.cs` com EXATAMENTE:

```csharp
namespace Diax.Application.Customers.Services;

/// <summary>
/// Resultado da checagem de entregabilidade do domínio de e-mail (EXTR-01).
/// </summary>
public enum MxCheckResult
{
    /// <summary>
    /// MX resolvido — ou, na ausência de MX, registro A do domínio (fallback RFC 5321,
    /// mesmo comportamento de `resolve_mx` em docs/email-marketing/mx_check.py).
    /// </summary>
    Valid = 0,

    /// <summary>
    /// Domínio comprovadamente sem entrega: NXDOMAIN, Null MX (RFC 7505) sem A, ou
    /// resposta vazia sem A. Este é o ÚNICO estado que rejeita o lead.
    /// </summary>
    NoMx = 1,

    /// <summary>
    /// Timeout / falha de infraestrutura DNS. Decisão D-02 do 07-CONTEXT.md:
    /// falha de infraestrutura NÃO é prova de lead ruim — o lead PASSA, marcado como
    /// "MX não verificado". Nunca tratar como NoMx.
    /// </summary>
    Unverified = 2
}

/// <summary>
/// Seam de I/O de rede para checagem de MX. Existe para que os filtros de qualidade do
/// ExtractorIntegrationService sejam testáveis offline (Mock&lt;IMxLookupService&gt;), no mesmo
/// padrão de IExtractorService / ICustomerRepository.
/// </summary>
public interface IMxLookupService
{
    /// <summary>
    /// Checa o domínio (parte depois do '@', sem '@', case-insensitive).
    /// NUNCA lança: qualquer falha de rede vira <see cref="MxCheckResult.Unverified"/>.
    /// </summary>
    Task<MxCheckResult> CheckAsync(string domain, CancellationToken cancellationToken = default);
}
```
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Include="DnsClient" Version="1.8.0"' api-core/src/Diax.Application/Diax.Application.csproj` retorna 1
    - `api-core/src/Diax.Application/Customers/Services/IMxLookupService.cs` contém as strings
      `enum MxCheckResult`, `Valid = 0`, `NoMx = 1`, `Unverified = 2`, `interface IMxLookupService`,
      `Task<MxCheckResult> CheckAsync`
    - `dotnet build -c Release` sai com exit code 0
  </acceptance_criteria>

  <done>
    Pacote DnsClient 1.8.0 restaurado e o contrato `IMxLookupService`/`MxCheckResult` compila.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MxResponseInterpreter (puro, testado) + DnsClientMxLookupService + DI</name>

  <files>
    api-core/src/Diax.Infrastructure/Dns/MxResponseInterpreter.cs (novo)
    api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs (novo)
    api-core/src/Diax.Infrastructure/DependencyInjection.cs
    api-core/tests/Diax.Tests/Customers/MxResponseInterpreterTests.cs (novo)
  </files>

  <read_first>
    - docs/email-marketing/mx_check.py — linhas 31-49 (`parse_nslookup_mx`, `resolve_mx`):
      a semântica sendo portada. Note o comentário `# Null MX "." => vazio` e o fallback
      para registro A.
    - api-core/src/Diax.Infrastructure/DependencyInjection.cs — linhas ~195-210 (bloco de
      registro de repositórios) e ~525-535 (bloco de `services.Configure<ExtractorPullOptions>`),
      para inserir os registros novos no lugar coerente e com o mesmo estilo.
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md — linhas 139-190
      (implementação de referência + opções do LookupClient) e linhas 308-321 (Pitfalls 1 e 2).
  </read_first>

  <behavior>
    `MxResponseInterpreter.InterpretMxResponse(mxExchanges, hasError, responseCode)`:
    - `(["mail.acme.com."], false, 0)` → `Valid`
    - `(["alt1.aspmx.l.google.com.", "alt2.aspmx.l.google.com."], false, 0)` → `Valid`
    - `([], true, 3)` → `NoMx` (NXDOMAIN — domínio não existe, nem tenta A)
    - `([], false, 0)` → `TryARecordFallback` (sem MX, mas domínio pode existir)
    - `(["."], false, 0)` → `TryARecordFallback` (Null MX RFC 7505 — trata como "sem MX")
    - `(["", "  "], false, 0)` → `TryARecordFallback` (exchanges vazios são ignorados)
    - `(["mail.acme.com."], true, 3)` → `Valid` (resposta com MX ganha de qualquer flag de erro)
    - `([], true, 2)` → `TryARecordFallback` (SERVFAIL não é NXDOMAIN — não afirmar "sem MX")

    `MxResponseInterpreter.InterpretAFallback(hasARecords)`:
    - `true` → `MxCheckResult.Valid`
    - `false` → `MxCheckResult.NoMx`
  </behavior>

  <action>
1. Criar `api-core/src/Diax.Infrastructure/Dns/MxResponseInterpreter.cs`:

```csharp
using Diax.Application.Customers.Services;

namespace Diax.Infrastructure.Dns;

/// <summary>Próximo passo depois de olhar a resposta MX.</summary>
public enum MxResolutionStep
{
    /// <summary>Há MX real — domínio entrega e-mail.</summary>
    Valid = 0,

    /// <summary>NXDOMAIN: o domínio não existe. Não vale nem consultar o registro A.</summary>
    NoMx = 1,

    /// <summary>Sem MX (ou Null MX): RFC 5321 permite cair no registro A do domínio.</summary>
    TryARecordFallback = 2
}

/// <summary>
/// Decisão pura sobre uma resposta DNS de MX, isolada do DnsClient.NET para ser testável sem
/// rede e sem construir tipos da lib. Porta de `parse_nslookup_mx` + `resolve_mx`
/// (docs/email-marketing/mx_check.py linhas 31-49).
/// </summary>
public static class MxResponseInterpreter
{
    /// <summary>RCODE 3 = NXDOMAIN (DnsResponseCode.NotExistentDomain).</summary>
    public const int NxDomainResponseCode = 3;

    /// <summary>Null MX (RFC 7505): único registro MX com Exchange igual à raiz.</summary>
    public const string NullMxExchange = ".";

    public static MxResolutionStep InterpretMxResponse(
        IReadOnlyList<string> mxExchanges,
        bool hasError,
        int responseCode)
    {
        var real = mxExchanges
            .Where(e => !string.IsNullOrWhiteSpace(e) && e.Trim() != NullMxExchange)
            .ToList();

        if (real.Count > 0)
            return MxResolutionStep.Valid;

        if (hasError && responseCode == NxDomainResponseCode)
            return MxResolutionStep.NoMx;

        return MxResolutionStep.TryARecordFallback;
    }

    public static MxCheckResult InterpretAFallback(bool hasARecords)
        => hasARecords ? MxCheckResult.Valid : MxCheckResult.NoMx;
}
```

2. Criar `api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs`:

```csharp
using Diax.Application.Customers.Services;
using DnsClient;
using Microsoft.Extensions.Logging;

namespace Diax.Infrastructure.Dns;

/// <summary>
/// Implementação real de IMxLookupService com DnsClient.NET 1.8.0 (decisão D-01).
/// Toda a decisão de negócio vive em MxResponseInterpreter (puro/testado); esta classe é só
/// o glue tipado com a lib.
/// </summary>
public class DnsClientMxLookupService : IMxLookupService
{
    private readonly ILookupClient _lookup;
    private readonly ILogger<DnsClientMxLookupService> _logger;

    public DnsClientMxLookupService(ILookupClient lookup, ILogger<DnsClientMxLookupService> logger)
    {
        _lookup = lookup;
        _logger = logger;
    }

    public async Task<MxCheckResult> CheckAsync(string domain, CancellationToken cancellationToken = default)
    {
        var d = (domain ?? string.Empty).Trim().TrimStart('@').ToLowerInvariant();
        if (string.IsNullOrEmpty(d))
            return MxCheckResult.NoMx;

        try
        {
            var mxResponse = await _lookup.QueryAsync(d, QueryType.MX, cancellationToken: cancellationToken);

            var exchanges = mxResponse.Answers.MxRecords()
                .Select(r => r.Exchange.Value)
                .ToList();

            var step = MxResponseInterpreter.InterpretMxResponse(
                exchanges,
                mxResponse.HasError,
                (int)mxResponse.Header.ResponseCode);

            if (step == MxResolutionStep.Valid) return MxCheckResult.Valid;
            if (step == MxResolutionStep.NoMx) return MxCheckResult.NoMx;

            // Sem MX / Null MX: RFC 5321 permite usar o registro A do próprio domínio.
            var aResponse = await _lookup.QueryAsync(d, QueryType.A, cancellationToken: cancellationToken);
            return MxResponseInterpreter.InterpretAFallback(aResponse.Answers.ARecords().Any());
        }
        catch (DnsResponseException ex) when (ex.Code == DnsResponseCode.ConnectionTimeout)
        {
            // D-02: falha de INFRAESTRUTURA não rejeita o lead.
            _logger.LogDebug("MX lookup de {Domain} expirou (ConnectionTimeout) — marcando como não verificado.", d);
            return MxCheckResult.Unverified;
        }
        catch (Exception ex)
        {
            // Qualquer outra falha (socket, DNS mal configurado no host, etc.) também é
            // infraestrutura — degrada para "não verificado", nunca para NoMx.
            _logger.LogWarning(ex, "MX lookup de {Domain} falhou de forma inesperada — marcando como não verificado.", d);
            return MxCheckResult.Unverified;
        }
    }
}
```

3. Em `api-core/src/Diax.Infrastructure/DependencyInjection.cs`, adicionar (perto do bloco de
registro de repositórios, ~linha 201, e com `using DnsClient;` + `using Diax.Infrastructure.Dns;`
+ `using Diax.Application.Customers.Services;` no topo se ainda não existirem):

```csharp
// ===== DNS / MX CHECK (EXTR-01) =====
// LookupClient é thread-safe e caro de construir (mantém sockets) → singleton, mesmo
// raciocínio de HttpClient. Timeout/Retries mais agressivos que o default (5s x 2) porque o
// pull diário processa até 1000 domínios por rodada.
services.AddSingleton<ILookupClient>(_ => new LookupClient(new LookupClientOptions
{
    Timeout = TimeSpan.FromSeconds(3),
    Retries = 1,
    UseCache = true,
    ThrowDnsErrors = false   // NÃO mudar: NXDOMAIN precisa voltar como resposta, não exceção,
                             // para ser distinguível de timeout (decisão D-02).
}));
services.AddScoped<IMxLookupService, DnsClientMxLookupService>();
```

4. Criar `api-core/tests/Diax.Tests/Customers/MxResponseInterpreterTests.cs` com um `[Theory]`
por caso de `<behavior>` acima. Assinatura sugerida:

```csharp
[Theory]
[MemberData(nameof(MxCases))]
public void InterpretMxResponse_MatchesPythonSemantics(
    string[] exchanges, bool hasError, int rcode, MxResolutionStep expected)
    => Assert.Equal(expected, MxResponseInterpreter.InterpretMxResponse(exchanges, hasError, rcode));
```
(`[InlineData]` não aceita `string[]` diretamente em todos os casos — usar `[MemberData]` ou
`[InlineData]` com `params`. Escolher o que compilar limpo.)
Mais 2 `[Fact]` para `InterpretAFallback(true/false)`.
`Assert.Equal` do xUnit. Sem FluentAssertions.

⚠️ **NÃO escrever testes que chamem `DnsClientMxLookupService.CheckAsync` contra DNS real** —
seriam não-determinísticos em CI e dependeriam de rede. O comportamento com rede real é validado
por smoke manual (ver 07-VALIDATION.md, seção "Manual-Only Verifications").
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~MxResponseInterpreterTests"</automated>
  </verify>

  <acceptance_criteria>
    - `api-core/src/Diax.Infrastructure/Dns/MxResponseInterpreter.cs` contém
      `NxDomainResponseCode = 3`, `NullMxExchange = "."`, `InterpretMxResponse`, `InterpretAFallback`
    - `api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs` contém
      `when (ex.Code == DnsResponseCode.ConnectionTimeout)` e `MxCheckResult.Unverified`
    - `grep -c "MxCheckResult.NoMx" api-core/src/Diax.Infrastructure/Dns/DnsClientMxLookupService.cs`
      retorna exatamente 1 (só o guard de domínio vazio — todo o resto vem do interpreter)
    - `grep -n "AddSingleton<ILookupClient>" api-core/src/Diax.Infrastructure/DependencyInjection.cs` acha 1 ocorrência
    - `grep -n "IMxLookupService, DnsClientMxLookupService" api-core/src/Diax.Infrastructure/DependencyInjection.cs` acha 1 ocorrência
    - `grep -c "ThrowDnsErrors = false" api-core/src/Diax.Infrastructure/DependencyInjection.cs` retorna 1
    - `MxResponseInterpreterTests.cs` NÃO contém `Should()` nem `FluentAssertions` nem `QueryAsync`
    - Comando de verify sai com exit code 0 e reporta ≥ 10 testes passando
  </acceptance_criteria>

  <done>
    `IMxLookupService` tem implementação real registrada no DI; a decisão Valid/NoMx/Unverified
    está coberta por testes determinísticos que não tocam a rede; timeout nunca produz `NoMx`.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build --filter "FullyQualifiedName~MxResponseInterpreterTests"
dotnet test -c Release --no-build   # suíte completa verde

# Smoke manual opcional (rede real, NÃO é gate automatizado):
python docs/email-marketing/mx_check.py   # tabela-verdade cruzada do lado Python
```
</verification>

<success_criteria>
- `DnsClient` 1.8.0 no `Diax.Application.csproj`, restore OK
- `IMxLookupService` mockável (`Mock<IMxLookupService>` compila nos testes)
- `LookupClient` registrado como **singleton** com `Timeout=3s`, `Retries=1`, `ThrowDnsErrors=false`
- Timeout → `Unverified` está coberto por código explícito e comentado com a referência a D-02
- Suíte completa verde (≥ 707 testes, 0 falhas)
- Nenhum consumidor de `IMxLookupService` foi criado ainda (isso é o plano 07-04)
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-02-SUMMARY.md`
</output>
