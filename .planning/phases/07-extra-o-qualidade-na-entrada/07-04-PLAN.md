---
phase: 07-extra-o-qualidade-na-entrada
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs
  - api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs
  - api-core/src/Diax.Application/DependencyInjection.cs
  - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
  - api-core/tests/Diax.Tests/Customers/CachedMxCheckServiceTests.cs
  - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs
autonomous: true
requirements: [EXTR-01]

must_haves:
  truths:
    - "Um lead cujo domínio de e-mail não tem MX nem A é rejeitado pelo worker .NET antes do import — não vira Customer"
    - "Um lead cujo domínio deu timeout de DNS PASSA (não é rejeitado) e é contado como 'não verificado' (D-02)"
    - "Um lead com domínio placeholder/lixo (instagram.local, wixpress.com) é rejeitado SEM nenhuma query DNS"
    - "Um domínio já checado dentro do TTL não dispara nova query DNS (cache persistente, D-03)"
    - "Se a taxa de 'não verificado' de uma rodada passar de 80%, um warning é logado (detecção precoce de DNS bloqueado no host)"
  artifacts:
    - path: "api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs"
      provides: "Camada de cache + pré-filtro de domínio-lixo sobre IMxLookupService"
      contains: "ICachedMxCheckService"
      min_lines: 80
    - path: "api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs"
      provides: "Toggles e TTLs do MX check"
      contains: "MxCheckEnabled"
    - path: "api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs"
      provides: "Filtro de MX no loop de import, com contadores"
      contains: "rejectedNoMx"
  key_links:
    - from: "api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs"
      to: "ICachedMxCheckService"
      via: "injeção no construtor + chamada no loop de ImportLeadsAsync"
      pattern: "ICachedMxCheckService"
    - from: "api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs"
      to: "IMxCacheRepository"
      via: "GetByDomainsAsync / AddAsync / UpdateAsync"
      pattern: "IMxCacheRepository"
    - from: "api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs"
      to: "JunkDomainFilter"
      via: "short-circuit antes de qualquer query DNS"
      pattern: "JunkDomainFilter.IsJunk"
---

<objective>
Plugar a checagem de MX no loop de import do `ExtractorIntegrationService` (EXTR-01), com cache
persistente por domínio (D-03), pré-filtro de domínio-lixo sem I/O, resolução em paralelo
limitado e instrumentação que detecta DNS bloqueado no host.

Purpose: é o requisito central da fase. Hoje só a ponte Python manual (`mx_check.py`) faz essa
checagem — o worker .NET (`ExtractorPullWorker`, diário 15:00 UTC) importa tudo. O bounce de 7,5%
da semana de 03-04/09 é o custo direto desse gap.

Output: `ICachedMxCheckService` + wiring no loop, com testes usando `Mock<IMxLookupService>` para
cobrir os 4 cenários (MX válido / sem MX / Null MX / timeout) sem tocar a rede.
</objective>

<decision_log>
**TTLs diferenciados por resultado (discretion do planner sobre D-03).**
`Valid`/`NoMx` cacheiam 30 dias (paridade com `CACHE_DAYS = 30` do `mx_check.py`). `Unverified`
cacheia apenas 24h — é um resultado de falha de infraestrutura, não um fato sobre o domínio;
manter por 30 dias congelaria uma indisponibilidade momentânea. Ambos são configuráveis em
`ExtractorPullOptions`.

**Paralelismo 8 (discretion do planner).** Mesmo grau de `check_many(workers=8)` no
`site_check.py` — consistência com o padrão Python já validado em produção. Com `Timeout=3s` e
`Retries=1` (07-02), o pior caso de 1000 domínios novos e 100% de timeout é ~1000/8 × 6s ≈ 12,5
min, aceitável para um `BackgroundService` diário.

**O plano NÃO assume que DNS de saída funciona no SmarterASP** (questão em aberto #2 da
pesquisa). Nenhum caminho de código depende disso: com UDP/53 bloqueado, tudo cai em `Unverified`
e o import segue normal. A instrumentação da Task 2 (warning se `Unverified` > 80% da rodada)
existe justamente para detectar esse cenário sem ter que confirmar nada com o provedor antes.
</decision_log>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-RESEARCH.md
@.planning/phases/07-extra-o-qualidade-na-entrada/07-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contratos exatos disponíveis quando este plano roda. Não explorar o codebase. -->

De 07-02 (`Diax.Application.Customers.Services`):
```csharp
public enum MxCheckResult { Valid = 0, NoMx = 1, Unverified = 2 }
public interface IMxLookupService {
    Task<MxCheckResult> CheckAsync(string domain, CancellationToken cancellationToken = default);
}
```

De 07-01 (`Diax.Application.Customers.Services`):
```csharp
public static class JunkDomainFilter {
    public static bool IsJunk(string? domain);       // recebe DOMÍNIO
    public static bool IsJunkEmail(string? email);   // recebe E-MAIL
}
```

De 07-03 (`Diax.Domain.Customers`):
```csharp
public class MxCacheEntry : AuditableEntity {
    public string Domain { get; }        // normalizado
    public int ResultCode { get; }       // 0=Valid, 1=NoMx, 2=Unverified (espelha MxCheckResult)
    public DateTime CheckedAt { get; }
    public MxCacheEntry(string domain, int resultCode);
    public void Refresh(int resultCode);
    public bool IsFresh(DateTime nowUtc, TimeSpan validFor);
    public static string Normalize(string? domain);
}
public interface IMxCacheRepository : IRepository<MxCacheEntry> {
    Task<MxCacheEntry?> GetByDomainAsync(string domain, CancellationToken ct = default);
    Task<IReadOnlyDictionary<string, MxCacheEntry>> GetByDomainsAsync(IReadOnlyCollection<string> domains, CancellationToken ct = default);
}
```

Estado ATUAL de `ExtractorIntegrationService.ImportLeadsAsync` (o que o executor vai modificar):
```csharp
// construtor atual (4 params):
public ExtractorIntegrationService(
    IExtractorService extractorService,
    CustomerImportService customerImportService,
    IOptions<ExtractorPullOptions> pullOptions,
    ILogger<ExtractorIntegrationService> logger)

// loop atual, dentro de `while (page <= maxPages)` → `foreach (var lead in leads)`:
var row = MapToImportRow(lead);
if (row == null) { skippedContactless++; continue; }
if (IsLowQualityEmail(row.Email)) { rejectedLowQuality++; continue; }
if (IsOutsideTargetGeo(lead)) { skippedByGeo++; continue; }
allLeads.Add(row);
```
Variáveis locais de contagem já existentes: `skippedContactless`, `rejectedLowQuality`,
`skippedByGeo`. Hoje elas só viram `_logger.LogInformation` — nunca persistem (esse gap é o
plano 07-06).

`IsLowQualityEmail(string? email)` é método privado do mesmo arquivo: rejeita `%` no endereço,
`_blockedDomains` (match exato do domínio) e `_blockedTlds` (sufixo). Retorna `false` para
e-mail nulo/vazio e para malformado sem domínio.

`api-core/src/Diax.Application/DependencyInjection.cs` linha 135-136:
```csharp
services.AddScoped<IExtractorService, ExtractorService>();
services.AddScoped<IExtractorIntegrationService, ExtractorIntegrationService>();
```

Teste existente `api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs`:
```csharp
private ExtractorIntegrationService CreateSut(ExtractorPullOptions? pullOptions = null) =>
    new(_extractorMock.Object,
        _importService,
        Options.Create(pullOptions ?? new ExtractorPullOptions()),
        Mock.Of<ILogger<ExtractorIntegrationService>>());
```
⚠️ Adicionar um parâmetro ao construtor QUEBRA `CreateSut` — atualizar o helper é parte da task,
e a suíte tem ~707 testes verdes que precisam continuar verdes.
Helpers existentes no arquivo: `SetupExtractorPage(page, leads, total)` e
`MakeLead(id, companyName, email)`.
⚠️ Assertions: `Assert.*` do xUnit puro. **FluentAssertions NÃO está em `Diax.Tests.csproj`.**

⚠️ Build/test SEMPRE com `-c Release`: `dotnet build -c Release` e depois
`dotnet test -c Release --no-build` (Smart App Control bloqueia DLL de teste Debug, `0x800711C7`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExtractorPullOptions (toggles/TTL) + CachedMxCheckService</name>

  <files>
    api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs
    api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs (novo)
    api-core/src/Diax.Application/DependencyInjection.cs
    api-core/tests/Diax.Tests/Customers/CachedMxCheckServiceTests.cs (novo)
  </files>

  <read_first>
    - api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs — LER INTEIRO (70 linhas).
      Note o estilo: comentário XML explicando o PORQUÊ de cada default, e o comentário de topo
      sobre override por env var `DIAX_ExtractorPull__*`.
    - docs/email-marketing/mx_check.py — linhas 10-12 (`CACHE_FILE`, `CACHE_DAYS = 30`) e
      linhas 52-70 (`deliverable_domain`: ordem exata junk → cache → resolver → grava cache).
      É a semântica sendo portada.
    - api-core/src/Diax.Domain/Customers/MxCacheEntry.cs — criado no plano 07-03.
    - api-core/src/Diax.Application/DependencyInjection.cs — linhas 130-140 (bloco onde
      `IExtractorService`/`IExtractorIntegrationService` são registrados).
  </read_first>

  <behavior>
    `CachedMxCheckService.CheckManyAsync(domains, ct)` devolve `IReadOnlyDictionary<string, MxCheckResult>`:
    - Domínio lixo (`instagram.local`) → `NoMx` **sem** nenhuma chamada a `IMxLookupService`
      (verificar com `_lookupMock.Verify(..., Times.Never)`)
    - Domínio com entrada em cache `Valid` de 1 dia atrás → `Valid`, **sem** chamada ao lookup
    - Domínio com entrada em cache `Valid` de 31 dias atrás → chama o lookup e faz `Refresh`
    - Domínio com entrada em cache `Unverified` de 2 horas atrás → `Unverified`, sem lookup
    - Domínio com entrada em cache `Unverified` de 25 horas atrás → chama o lookup (TTL de
      Unverified é 24h, não 30 dias)
    - Domínio sem cache → chama o lookup uma vez e persiste uma `MxCacheEntry` nova
    - O mesmo domínio repetido 5x na lista de entrada → 1 única chamada ao lookup
    - `MxCheckEnabled = false` nas options → todos os domínios voltam `Unverified`, zero chamadas
      ao lookup, zero escrita no cache
    - Lista vazia → dicionário vazio, zero chamadas
    - Resultado `Unverified` vindo do lookup → **é** persistido no cache (com TTL curto)
  </behavior>

  <action>
1. Em `api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs`, adicionar ao final da
   classe (mantendo o estilo de comentário XML dos campos existentes):

```csharp
    /// <summary>
    /// Liga/desliga a checagem de MX no import do Extrator (EXTR-01). Default: true.
    /// Desligar faz todo lead ser tratado como "MX não verificado" — nada é rejeitado por MX.
    /// </summary>
    public bool MxCheckEnabled { get; set; } = true;

    /// <summary>
    /// TTL do cache de MX para resultados conclusivos (Valid / NoMx), em dias.
    /// Default 30 — paridade com CACHE_DAYS do docs/email-marketing/mx_check.py.
    /// </summary>
    public int MxCacheDays { get; set; } = 30;

    /// <summary>
    /// TTL do cache de MX para o resultado "não verificado" (timeout/falha de infraestrutura),
    /// em horas. Default 24 — muito menor que MxCacheDays porque Unverified é um fato sobre a
    /// REDE naquele momento, não sobre o domínio; congelar por 30 dias perpetuaria uma
    /// indisponibilidade momentânea.
    /// </summary>
    public int MxUnverifiedCacheHours { get; set; } = 24;

    /// <summary>
    /// Quantas queries DNS rodam em paralelo por rodada de import. Default 8 — mesmo grau de
    /// check_many(workers=8) do docs/email-marketing/site_check.py.
    /// </summary>
    public int MxLookupParallelism { get; set; } = 8;
```

2. Criar `api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs`:

```csharp
using Diax.Domain.Common;
using Diax.Domain.Customers;
using Microsoft.Extensions.Logging;
using Microsoft.Extensions.Options;

namespace Diax.Application.Customers.Services;

/// <summary>
/// Checagem de MX em lote com cache persistente por domínio (EXTR-01 / D-03).
/// Ordem de decisão — porta de `deliverable_domain` em docs/email-marketing/mx_check.py:
///   1. domínio lixo/placeholder  → NoMx, custo zero (nenhuma query DNS)
///   2. cache fresco              → resultado cacheado, custo zero
///   3. resolução DNS real        → IMxLookupService, com paralelismo limitado
///   4. grava/atualiza o cache
/// </summary>
public interface ICachedMxCheckService
{
    /// <summary>
    /// Resolve um conjunto de domínios. A chave do dicionário devolvido é o domínio
    /// NORMALIZADO (MxCacheEntry.Normalize). Nunca lança.
    /// </summary>
    Task<IReadOnlyDictionary<string, MxCheckResult>> CheckManyAsync(
        IReadOnlyCollection<string> domains,
        CancellationToken cancellationToken = default);
}

public class CachedMxCheckService : ICachedMxCheckService
{
    private readonly IMxLookupService _lookup;
    private readonly IMxCacheRepository _cache;
    private readonly IUnitOfWork _unitOfWork;
    private readonly ExtractorPullOptions _options;
    private readonly ILogger<CachedMxCheckService> _logger;

    public CachedMxCheckService(
        IMxLookupService lookup,
        IMxCacheRepository cache,
        IUnitOfWork unitOfWork,
        IOptions<ExtractorPullOptions> options,
        ILogger<CachedMxCheckService> logger)
    {
        _lookup = lookup;
        _cache = cache;
        _unitOfWork = unitOfWork;
        _options = options.Value;
        _logger = logger;
    }

    public async Task<IReadOnlyDictionary<string, MxCheckResult>> CheckManyAsync(
        IReadOnlyCollection<string> domains,
        CancellationToken cancellationToken = default)
    {
        var results = new Dictionary<string, MxCheckResult>(StringComparer.Ordinal);

        var normalized = domains
            .Select(MxCacheEntry.Normalize)
            .Where(d => !string.IsNullOrEmpty(d))
            .Distinct(StringComparer.Ordinal)
            .ToList();

        if (normalized.Count == 0)
            return results;

        if (!_options.MxCheckEnabled)
        {
            foreach (var d in normalized)
                results[d] = MxCheckResult.Unverified;
            return results;
        }

        // 1. Domínio lixo: decisão sem I/O nenhum.
        var toResolve = new List<string>();
        foreach (var d in normalized)
        {
            if (JunkDomainFilter.IsJunk(d))
                results[d] = MxCheckResult.NoMx;
            else
                toResolve.Add(d);
        }

        if (toResolve.Count == 0)
            return results;

        // 2. Cache fresco.
        var cached = await _cache.GetByDomainsAsync(toResolve, cancellationToken);
        var now = DateTime.UtcNow;
        var stillToResolve = new List<string>();

        foreach (var d in toResolve)
        {
            if (cached.TryGetValue(d, out var entry) && entry.IsFresh(now, TtlFor(entry.ResultCode)))
                results[d] = (MxCheckResult)entry.ResultCode;
            else
                stillToResolve.Add(d);
        }

        if (stillToResolve.Count == 0)
            return results;

        // 3. Resolução DNS real, paralelismo limitado.
        var resolved = new System.Collections.Concurrent.ConcurrentDictionary<string, MxCheckResult>(StringComparer.Ordinal);
        var maxDop = Math.Max(1, _options.MxLookupParallelism);

        await Parallel.ForEachAsync(
            stillToResolve,
            new ParallelOptions { MaxDegreeOfParallelism = maxDop, CancellationToken = cancellationToken },
            async (domain, ct) =>
            {
                var r = await _lookup.CheckAsync(domain, ct);
                resolved[domain] = r;
            });

        // 4. Persiste o cache (inclusive Unverified — com TTL curto — para não martelar o mesmo
        //    domínio problemático a cada rodada).
        foreach (var (domain, result) in resolved)
        {
            results[domain] = result;

            if (cached.TryGetValue(domain, out var existing))
            {
                existing.Refresh((int)result);
                await _cache.UpdateAsync(existing, cancellationToken);
            }
            else
            {
                await _cache.AddAsync(new MxCacheEntry(domain, (int)result), cancellationToken);
            }
        }

        await _unitOfWork.SaveChangesAsync(cancellationToken);

        _logger.LogInformation(
            "Checagem de MX: {Total} domínio(s) distinto(s) — {Junk} lixo, {Cached} de cache, {Resolved} resolvido(s) por DNS.",
            normalized.Count,
            normalized.Count - toResolve.Count,
            toResolve.Count - stillToResolve.Count,
            stillToResolve.Count);

        return results;
    }

    private TimeSpan TtlFor(int resultCode) =>
        resultCode == (int)MxCheckResult.Unverified
            ? TimeSpan.FromHours(Math.Max(1, _options.MxUnverifiedCacheHours))
            : TimeSpan.FromDays(Math.Max(1, _options.MxCacheDays));
}
```
   ⚠️ Conferir o namespace/assinatura reais de `IUnitOfWork` (`SaveChangesAsync(CancellationToken)`)
   lendo `api-core/src/Diax.Domain/Common/IUnitOfWork.cs` ou o `using` já usado em
   `CustomerImportService.cs`; ajustar o `using` se necessário.

3. Em `api-core/src/Diax.Application/DependencyInjection.cs`, junto das linhas 135-136:
```csharp
        services.AddScoped<ICachedMxCheckService, CachedMxCheckService>();
```

4. Criar `api-core/tests/Diax.Tests/Customers/CachedMxCheckServiceTests.cs` com um `[Fact]` por
   caso de `<behavior>` (10 fatos), usando:
   - `Mock<IMxLookupService>`, `Mock<IMxCacheRepository>`, `Mock<IUnitOfWork>`,
     `Options.Create(new ExtractorPullOptions { ... })`, `Mock.Of<ILogger<CachedMxCheckService>>()`
   - Para simular entradas de cache com idade: construir `new MxCacheEntry(domain, code)` e
     verificar via a JANELA DE TTL passada nas options em vez de forjar `CheckedAt` — ex.: para
     "cache de 31 dias atrás", setar `MxCacheDays = 0`... **NÃO**. Em vez disso, expor a idade via
     o próprio `IsFresh` já é impossível de forjar. Solução obrigatória: no mock de
     `GetByDomainsAsync`, devolver uma `MxCacheEntry` construída e, para os casos "expirado",
     configurar as options com TTL curtíssimo (`MxCacheDays = 1` não serve). **Usar
     `Mock<MxCacheEntry>` também não serve** (props não são virtuais).
     → Implementar `MxCacheEntry.Refresh` já resolve o caso fresco. Para o caso EXPIRADO, tornar
     o TTL efetivo zero: `MxUnverifiedCacheHours` e `MxCacheDays` são `Math.Max(1, ...)`, então
     o mínimo é 1 hora / 1 dia. Portanto, para testar expiração de forma determinística,
     **adicionar em `MxCacheEntry` um construtor de teste**:
     `public MxCacheEntry(string domain, int resultCode, DateTime checkedAtUtc)` (público,
     documentado como "usado por seeds/testes; produção usa o construtor de 2 args") e usar esse
     construtor nos testes para criar entradas com `CheckedAt = DateTime.UtcNow.AddDays(-31)` /
     `.AddHours(-25)`. Adicionar esse construtor a
     `api-core/src/Diax.Domain/Customers/MxCacheEntry.cs` como parte desta task.
   - `Verify(x => x.CheckAsync(It.IsAny<string>(), It.IsAny<CancellationToken>()), Times.Never())`
     para os casos de custo zero.
   - `Assert.*` do xUnit puro. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~CachedMxCheckServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `api-core/src/Diax.Application/Customers/ExtractorPullOptions.cs` contém as 4 strings
      `MxCheckEnabled`, `MxCacheDays`, `MxUnverifiedCacheHours`, `MxLookupParallelism`
    - `api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs` contém
      `JunkDomainFilter.IsJunk`, `Parallel.ForEachAsync`, `MaxDegreeOfParallelism`, `IMxCacheRepository`
    - `grep -c "MxCheckResult.NoMx" api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs` retorna 1 (só o short-circuit de domínio-lixo)
    - `grep -n "IsJunk" api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs` aparece ANTES da linha de `GetByDomainsAsync` (junk é checado antes do cache — custo zero)
    - `grep -n "ICachedMxCheckService, CachedMxCheckService" api-core/src/Diax.Application/DependencyInjection.cs` acha 1 ocorrência
    - `api-core/src/Diax.Domain/Customers/MxCacheEntry.cs` contém `DateTime checkedAtUtc` (construtor de 3 args)
    - Comando de verify sai com exit code 0 e reporta ≥ 10 testes passando
  </acceptance_criteria>

  <done>
    `ICachedMxCheckService` resolve um lote de domínios com short-circuit de lixo, cache com TTL
    diferenciado por resultado e paralelismo limitado — coberto por testes que provam quantas
    vezes o DNS foi (ou não foi) consultado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring do filtro de MX no loop do ExtractorIntegrationService + instrumentação</name>

  <files>
    api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
    api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs
  </files>

  <read_first>
    - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs — LER INTEIRO
      (~300 linhas). Entender: construtor, o `while (page <= maxPages)` com o `foreach` por lead,
      os contadores locais, os blocos de `_logger.LogInformation` depois do loop, e
      `MapToImportRow`.
    - api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs — LER as linhas
      1-110 (setup do fixture, `_importService`, `CreateSut`) e as linhas 580-730 (exemplos de
      testes que usam `CreateSut(new ExtractorPullOptions { ... })` — molde para os novos).
    - api-core/src/Diax.Application/Customers/Services/CachedMxCheckService.cs — criado na Task 1.
    - .planning/phases/07-extra-o-qualidade-na-entrada/07-CONTEXT.md — decisão D-02 (o motivo de
      timeout NÃO rejeitar).
  </read_first>

  <behavior>
    - Lote com 2 leads, `IMxLookupService` mockado devolvendo `Valid` para ambos → ambos importados,
      `SuccessCount == 2`
    - Lote com 1 lead cujo domínio devolve `NoMx` → lead NÃO é importado; se for o único lead do
      lote, o resultado é `Result.Failure` com código `ExtractorImport.NoLeads`
    - Lote com 2 leads, um `Valid` e um `NoMx` → só o `Valid` é importado, `SuccessCount == 1`
    - Lote com 1 lead cujo domínio devolve `Unverified` (timeout) → lead **É** importado
      (`SuccessCount == 1`) — D-02
    - Lead com e-mail `contato@instagram.local` → rejeitado, e `IMxLookupService.CheckAsync`
      NUNCA é chamado para esse domínio
    - Lead sem e-mail (só telefone) → o MX check não é aplicado; o comportamento pré-existente
      (rejeição a jusante por e-mail inválido) não muda
    - `MxCheckEnabled = false` nas options → nenhum lead é rejeitado por MX; comportamento
      idêntico ao de antes desta fase
    - Todos os ~707 testes pré-existentes continuam verdes
  </behavior>

  <action>
1. Em `ExtractorIntegrationService.cs`:

   a) Injetar `ICachedMxCheckService` no construtor (5º parâmetro, DEPOIS de
      `IOptions<ExtractorPullOptions> pullOptions` e ANTES de `ILogger`), guardando em
      `private readonly ICachedMxCheckService _mxCheck;`. Guardar também
      `private readonly ExtractorPullOptions _options = pullOptions.Value;` se ainda não houver
      um campo com as options completas (hoje o construtor só extrai as 4 listas).

   b) Em `ImportLeadsAsync`, adicionar um contador local ao lado dos existentes:
```csharp
        var rejectedNoMx = 0;
        var mxUnverified = 0;
```

   c) **Não** chamar o MX check dentro do `foreach` por lead (seria sequencial). Em vez disso,
      reestruturar assim: manter o loop de paginação como está, mas em vez de `allLeads.Add(row)`
      direto, acumular pares `(row, lead)` numa lista `candidates`. Depois de sair do `while`
      de paginação, fazer UMA resolução em lote e então filtrar:

```csharp
        // ── EXTR-01: checagem de MX em lote, DEPOIS da paginação ────────────────
        // Em lote (não por lead) para aproveitar cache + paralelismo. Domínios distintos:
        // ~1000 leads/rodada costumam ter uma fração disso em domínios únicos.
        var domains = candidates
            .Select(c => ExtractDomain(c.Row.Email))
            .Where(d => !string.IsNullOrEmpty(d))
            .Distinct(StringComparer.OrdinalIgnoreCase)
            .ToList();

        var mxResults = await _mxCheck.CheckManyAsync(domains, cancellationToken);

        foreach (var candidate in candidates)
        {
            var domain = ExtractDomain(candidate.Row.Email);

            // Lead sem e-mail não é julgado por MX — a validação de contato é a jusante.
            if (!string.IsNullOrEmpty(domain) && mxResults.TryGetValue(domain, out var mx))
            {
                if (mx == MxCheckResult.NoMx)
                {
                    rejectedNoMx++;
                    continue;
                }

                if (mx == MxCheckResult.Unverified)
                    mxUnverified++;   // D-02: PASSA — falha de infra não é prova de lead ruim
            }

            allLeads.Add(candidate.Row);
        }
```
      Adicionar o helper privado:
```csharp
    /// <summary>Domínio normalizado de um e-mail (lowercase, sem '@'). Vazio se não houver.</summary>
    private static string ExtractDomain(string? email)
    {
        if (string.IsNullOrWhiteSpace(email)) return string.Empty;
        var at = email.LastIndexOf('@');
        if (at < 0 || at == email.Length - 1) return string.Empty;
        return email[(at + 1)..].Trim().ToLowerInvariant();
    }
```
      ⚠️ `CheckManyAsync` normaliza a chave com `MxCacheEntry.Normalize` (trim + TrimStart('@') +
      lowercase). `ExtractDomain` produz exatamente o mesmo formato, então o `TryGetValue` casa.
      O dicionário devolvido usa `StringComparer.Ordinal` — por isso `ExtractDomain` **precisa**
      devolver lowercase.

   d) Depois do loop, adicionar os logs (no mesmo estilo dos blocos `if (skippedByGeo > 0)` já
      existentes):
```csharp
        if (rejectedNoMx > 0)
        {
            _logger.LogInformation(
                "Filtro de MX: {Rejected} lead(s) rejeitado(s) — domínio sem MX e sem registro A (EXTR-01).",
                rejectedNoMx);
        }

        // Instrumentação (questão em aberto #2 da pesquisa): DNS de saída no SmarterASP é
        // indocumentado. Se UDP/53 estiver bloqueado, TODA checagem cai em Unverified e a
        // feature silenciosamente não filtra nada. Este warning torna isso visível na primeira
        // rodada, sem depender de confirmação do provedor.
        var mxEvaluated = rejectedNoMx + mxUnverified + allLeads.Count;
        if (mxEvaluated > 0 && mxUnverified * 100 / mxEvaluated > 80)
        {
            _logger.LogWarning(
                "ALERTA DE INFRAESTRUTURA: {Unverified}/{Total} ({Pct}%) das checagens de MX voltaram " +
                "'não verificado'. Isso indica DNS de saída bloqueado ou instável no host — o filtro " +
                "de MX está efetivamente desligado nesta rodada (nenhum lead foi perdido, per D-02).",
                mxUnverified, mxEvaluated, mxUnverified * 100 / mxEvaluated);
        }
```

   e) `IsLowQualityEmail` passa a incluir o pré-filtro de domínio-lixo. Logo depois do bloco de
      `_blockedTlds`, ANTES do `return false;`, adicionar:
```csharp
        // Domínio placeholder / infraestrutura de terceiro (instagram.local, wixpress.com,
        // sentry.io...). Porta de is_junk_domain do mx_check.py — conta no bucket "e-mail lixo",
        // não no bucket "sem MX" (preserva os 4 buckets de D-04).
        if (JunkDomainFilter.IsJunk(domain))
            return true;
```
      (o `domain` local já existe nesse método). Isso garante que domínio-lixo seja rejeitado
      cedo, contado como `rejectedLowQuality`, e nunca chegue ao MX check.

2. Em `ExtractorIntegrationServiceTests.cs`:

   a) Adicionar `private readonly Mock<ICachedMxCheckService> _mxCheckMock = new();` aos campos.

   b) No construtor do fixture, configurar o default "tudo Valid":
```csharp
        _mxCheckMock
            .Setup(m => m.CheckManyAsync(It.IsAny<IReadOnlyCollection<string>>(), It.IsAny<CancellationToken>()))
            .ReturnsAsync((IReadOnlyCollection<string> ds, CancellationToken _) =>
                ds.ToDictionary(d => d, _ => MxCheckResult.Valid, StringComparer.Ordinal));
```
      Isso mantém os ~707 testes existentes verdes sem editar nenhum deles.

   c) Atualizar `CreateSut` para passar `_mxCheckMock.Object` na nova posição do construtor.

   d) Adicionar um `[Fact]` por caso de `<behavior>` (8 fatos), usando os helpers existentes
      `SetupExtractorPage` e `MakeLead`. Para o caso de `instagram.local`, assertar
      `_mxCheckMock.Verify(m => m.CheckManyAsync(It.Is<IReadOnlyCollection<string>>(ds => !ds.Contains("instagram.local")), It.IsAny<CancellationToken>()), Times.Once())`.
   `Assert.*` do xUnit puro. Sem FluentAssertions.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release && dotnet test -c Release --no-build --filter "FullyQualifiedName~ExtractorIntegrationServiceTests"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ICachedMxCheckService" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna ≥ 3
    - `grep -c "rejectedNoMx" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna ≥ 3
    - `grep -c "mxUnverified" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna ≥ 3
    - `api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` contém `JunkDomainFilter.IsJunk` e `ALERTA DE INFRAESTRUTURA`
    - `grep -c "MxCheckResult.Unverified" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` retorna ≥ 1 e o `continue` do bloco de rejeição está associado a `MxCheckResult.NoMx`, NÃO a `Unverified` (validar lendo o bloco — timeout jamais rejeita, D-02)
    - `grep -c "_mxCheckMock" api-core/tests/Diax.Tests/Customers/ExtractorIntegrationServiceTests.cs` retorna ≥ 4
    - `dotnet test -c Release --no-build` (suíte COMPLETA) sai com exit code 0, ≥ 715 testes, 0 falhas
  </acceptance_criteria>

  <done>
    Lead com domínio sem MX/A é rejeitado pelo worker .NET antes do import; lead com timeout de
    DNS passa e é contado como não verificado; domínio-lixo é rejeitado sem query DNS; a suíte
    inteira continua verde.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core
dotnet build -c Release
dotnet test -c Release --no-build --filter "FullyQualifiedName~CachedMxCheckServiceTests"
dotnet test -c Release --no-build --filter "FullyQualifiedName~ExtractorIntegrationServiceTests"
dotnet test -c Release --no-build          # suíte completa, 0 falhas
```
</verification>

<success_criteria>
- EXTR-01 satisfeito: domínio sem MX/A rejeitado ANTES de virar `Customer`, no worker .NET
- D-02 honrada por código explícito: `Unverified` nunca entra no `continue` de rejeição
- Cache persistente ativo: domínio repetido/já visto não dispara nova query
- Domínio-lixo rejeitado com custo zero de I/O, contado no bucket "e-mail lixo"
- Warning de infraestrutura dispara quando `Unverified` > 80% da rodada
- Suíte completa verde (≥ 715 testes, 0 falhas); nenhum teste pré-existente editado por causa de quebra
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/07-extra-o-qualidade-na-entrada/07-04-SUMMARY.md`
</output>
