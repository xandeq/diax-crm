---
phase: 08-import-—-dedup-e-score-em-tempo-real
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-core/src/Diax.Domain/Customers/ICustomerRepository.cs
  - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs
  - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs
  - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
  - api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs
autonomous: true
requirements: [IMPT-01, IMPT-02]

must_haves:
  truths:
    - "Existe uma forma de buscar um Customer pelo ExternalId do Extrator (hoje só existe busca por e-mail, documento e telefone)"
    - "Uma ImportCustomerRow consegue carregar o lead.Id do Extrator até o CustomerImportService sem que nenhum chamador posicional existente quebre"
    - "MapToImportRow do ExtractorIntegrationService propaga lead.Id como ExternalId, não só como texto dentro de Notes"
    - "Buscar por um ExternalId inexistente devolve null em vez de lançar"
  artifacts:
    - path: "api-core/src/Diax.Domain/Customers/ICustomerRepository.cs"
      provides: "Contrato GetByExternalIdAsync na única superfície de query de Customer"
      contains: "GetByExternalIdAsync"
    - path: "api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs"
      provides: "Implementação EF Core de GetByExternalIdAsync espelhando GetByEmailAsync"
      contains: "c.ExternalId == externalId"
    - path: "api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs"
      provides: "Campo ExternalId opcional, último parâmetro posicional do record"
      contains: "string? ExternalId = null"
    - path: "api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs"
      provides: "Cobertura InMemory de GetByExternalIdAsync (hit, miss, e convivência de múltiplos NULL)"
      min_lines: 40
  key_links:
    - from: "api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs"
      to: "ImportCustomerRow.ExternalId"
      via: "MapToImportRow passando lead.Id.ToString()"
      pattern: "ExternalId: lead.Id"
    - from: "api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs"
      to: "customers.external_id"
      via: "FirstOrDefaultAsync sobre a coluna já indexada (IX_Customers_ExternalId)"
      pattern: "GetByExternalIdAsync"
---

<objective>
Criar os contratos que a lógica de dedup por `ExternalId` vai consumir na Wave 2: método de
repositório, campo no DTO de import e a fiação que leva o `lead.Id` do Extrator até lá.

Purpose: hoje o `lead.Id` do Extrator morre como texto dentro de `Notes`
(`"ID Extrator: {lead.Id}"`, `ExtractorIntegrationService.cs:361-362`) e o
`ICustomerRepository` não tem nenhuma forma de buscar por ele. Sem esses três contratos, a
resolução de dedup do plano 08-03 não tem onde se apoiar. Este plano é deliberadamente
"interface-first": nenhuma regra de negócio muda aqui, então ele roda em paralelo com o 08-02.

Output: `ICustomerRepository.GetByExternalIdAsync` + implementação EF Core,
`ImportCustomerRow.ExternalId` (último parâmetro, nullable, default null),
`MapToImportRow` propagando `lead.Id`, e um teste InMemory do novo método de repositório.

⚠️ **NÃO gerar migration.** A coluna `customers.external_id` e o índice único filtrado
`IX_Customers_ExternalId` (`WHERE [external_id] IS NOT NULL`) já foram criados pela migration
`20260906101839_AddLeadQualitySignals.cs` na Phase 7 e já estão aplicados em produção. Rodar
`add-migration.ps1` aqui produziria um `Up()` vazio ou uma colisão. A Phase 8 inteira tem ZERO
migrations.
</objective>

<execution_context>
@C:/Users/acq20/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/acq20/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-import-—-dedup-e-score-em-tempo-real/08-CONTEXT.md
@.planning/phases/08-import-—-dedup-e-score-em-tempo-real/08-RESEARCH.md
@.planning/phases/08-import-—-dedup-e-score-em-tempo-real/08-VALIDATION.md

<project_rules>
- Build e test SEMPRE com `-c Release`. DLL de teste em Debug é bloqueada pelo Smart App Control (`0x800711C7`).
- O projeto de teste NÃO referencia FluentAssertions — `Should()` NÃO COMPILA. Usar xUnit `Assert` + Moq.
- Baseline da suíte completa: **868 testes** (medido em 2026-09-07, depois dos PRs #106/#107/#109,
  que mexeram só em autenticação e não tocam nenhum arquivo desta fase). Nenhum plano pode regredir isso.
- NUNCA `git add -A` / `git add .` / `commit -a` neste repo — outra sessão trabalha na mesma
  working tree e há muitos arquivos sujos não relacionados. Stagear caminhos explícitos.
- A working tree local deve permanecer na branch `chore/email-automation-versioned`.
</project_rules>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real. Não explorar o codebase. -->

Padrão a espelhar — `api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs:16-32`:
```csharp
public async Task<Customer?> GetByEmailAsync(string email, CancellationToken cancellationToken = default)
{
    return await DbSet
        .FirstOrDefaultAsync(c => c.Email == email, cancellationToken);
}

public async Task<Customer?> GetByPhoneAsync(string phone, CancellationToken cancellationToken = default)
{
    var normalized = phone.Trim();
    return await DbSet
        .FirstOrDefaultAsync(c => c.Phone == normalized || c.WhatsApp == normalized, cancellationToken);
}
```

Estado atual do record — `api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs:9-22`
(13 parâmetros posicionais; existem chamadores posicionais em `ExtractorIntegrationService`,
`ApifyIntegrationService` e em ~30 testes):
```csharp
public record ImportCustomerRow(
    string Name, string Email, string? Phone = null, string? WhatsApp = null,
    string? CompanyName = null, string? Notes = null, string? Tags = null,
    string? Website = null, string? City = null, string? CurrentTool = null,
    string? MainPain = null, string? ValidationStatus = null, string? ConsentStatus = null);
```

Já existe no domínio — `api-core/src/Diax.Domain/Customers/Customer.cs:86` e `:298-301`:
```csharp
public string? ExternalId { get; private set; }

public void SetExternalId(string? externalId)
{
    ExternalId = string.IsNullOrWhiteSpace(externalId) ? null : externalId.Trim();
}
```

Tipo da chave de origem — `ExtractorLead.Id` é `long` (`ExtractorService.cs:247`), e a coluna é
`nvarchar(64)`. O mapeamento correto é `lead.Id.ToString()`.

Padrão de teste InMemory a espelhar — `api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs:23-35`:
```csharp
private sealed class FakeCurrentUserService : ICurrentUserService
{
    public FakeCurrentUserService(Guid userId) { UserId = userId; }
    public Guid? UserId { get; }
    public bool IsAuthenticated => UserId.HasValue;
}

private static DiaxDbContext CreateDbContext()
{
    var options = new DbContextOptionsBuilder<DiaxDbContext>()
        .UseInMemoryDatabase($"import-history-{Guid.NewGuid()}")
        .Options;
    return new DiaxDbContext(options, new FakeCurrentUserService(Guid.NewGuid()));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contratos — GetByExternalIdAsync, ImportCustomerRow.ExternalId e fiação do MapToImportRow</name>

  <files>
    api-core/src/Diax.Domain/Customers/ICustomerRepository.cs,
    api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs,
    api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs,
    api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs
  </files>

  <read_first>
    - api-core/src/Diax.Domain/Customers/ICustomerRepository.cs (ver o bloco GetByEmailAsync/GetByDocumentAsync/GetByPhoneAsync, linhas 11-24)
    - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs (linhas 1-45 — o padrão exato a copiar)
    - api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs (record completo)
    - api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs (método MapToImportRow, linhas 327-381)
    - api-core/src/Diax.Domain/Customers/Customer.cs (linhas 79-86 e 293-301 — ExternalId + SetExternalId já existentes)
    - api-core/src/Diax.Infrastructure/Data/Configurations/CustomerConfiguration.cs (linhas 135-150 — confirmar que o índice já existe e NÃO recriar)
  </read_first>

  <action>
1. **`ICustomerRepository.cs`** — adicionar, logo abaixo do `GetByPhoneAsync` (linha ~24), o método:
```csharp
    /// <summary>
    /// Busca cliente pelo ID durável da fonte externa (hoje: `lead.Id` do Extrator de Dados).
    /// Chave de dedup cross-pull para leads que trocam de e-mail entre passadas do scraper (IMPT-01).
    /// </summary>
    Task<Customer?> GetByExternalIdAsync(string externalId, CancellationToken cancellationToken = default);
```

2. **`CustomerRepository.cs`** — adicionar a implementação imediatamente após `GetByPhoneAsync`
   (linha ~33), espelhando o padrão de `GetByEmailAsync` (normalização com `Trim()` porque
   `Customer.SetExternalId` grava sempre trimado):
```csharp
    public async Task<Customer?> GetByExternalIdAsync(string externalId, CancellationToken cancellationToken = default)
    {
        var normalized = externalId.Trim();
        return await DbSet
            .FirstOrDefaultAsync(c => c.ExternalId == normalized, cancellationToken);
    }
```

3. **`BulkImportDtos.cs`** — adicionar `ExternalId` como **ÚLTIMO** parâmetro posicional do record
   `ImportCustomerRow`, nullable e com default `null`. Inserir em qualquer outra posição quebra
   os ~30 chamadores posicionais existentes em tempo de compilação:
```csharp
public record ImportCustomerRow(
    string Name,
    string Email,
    string? Phone = null,
    string? WhatsApp = null,
    string? CompanyName = null,
    string? Notes = null,
    string? Tags = null,
    string? Website = null,
    string? City = null,
    string? CurrentTool = null,
    string? MainPain = null,
    string? ValidationStatus = null,
    string? ConsentStatus = null,
    string? ExternalId = null);   // IMPT-01: lead.Id do Extrator. SEMPRE o último parâmetro.
```

4. **`ExtractorIntegrationService.cs` / `MapToImportRow`** — no `return new ImportCustomerRow(...)`
   (linhas ~370-380), acrescentar o argumento nomeado `ExternalId`. **Manter** a linha
   `noteParts.Add($"ID Extrator: {lead.Id}")` (legibilidade humana em `Notes`) — ela não é a
   chave de dedup, o campo novo é:
```csharp
        return new ImportCustomerRow(
            Name: name,
            Email: lead.Email ?? string.Empty,
            Phone: lead.Phone,
            WhatsApp: lead.WhatsApp ?? lead.Phone,
            CompanyName: lead.CompanyName,
            Notes: noteParts.Count > 0 ? string.Join("\n", noteParts) : null,
            Tags: string.Join(",", tags),
            Website: lead.Website,       // EXTR-03
            ExternalId: lead.Id > 0 ? lead.Id.ToString() : null   // IMPT-01: chave de dedup durável
        );
```

5. **Atualizar o comentário obsoleto** do bloco `// IDEMPOTÊNCIA:` em
   `ExtractorIntegrationService.cs:337-346`. A última linha dele diz hoje:
   `"Nota: dedup durável por lead.Id (coluna ExternalId) fica como recomendação (migração em prod)."`
   Substituir por:
   `"IMPT-01: o lead.Id agora viaja em ImportCustomerRow.ExternalId e é a PRIMEIRA chave de dedup tentada pelo CustomerImportService (antes de e-mail e telefone)."`

6. **NÃO** rodar `add-migration.ps1` nem `update-db.ps1`. Nenhuma mudança de schema neste plano.
  </action>

  <verify>
    <automated>cd api-core && dotnet build -c Release</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "GetByExternalIdAsync" api-core/src/Diax.Domain/Customers/ICustomerRepository.cs` retorna 1
    - `grep -q "c.ExternalId == normalized" api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs` é verdadeiro
    - `grep -q "string? ExternalId = null);" api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs` é verdadeiro
    - Em `BulkImportDtos.cs`, `ExternalId` é o último parâmetro do record `ImportCustomerRow` (a linha imediatamente anterior é `string? ConsentStatus = null,`)
    - `grep -q "ExternalId: lead.Id > 0 ? lead.Id.ToString() : null" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` é verdadeiro
    - `grep -q "ID Extrator: {lead.Id}" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` continua verdadeiro (a nota humana NÃO foi removida)
    - `grep -q "migração em prod" api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs` é FALSO (comentário obsoleto substituído)
    - `git status --porcelain api-core/src/Diax.Infrastructure/Data/Migrations/` não lista nenhum arquivo novo
    - `cd api-core && dotnet build -c Release` termina com `Build succeeded` e 0 erros
  </acceptance_criteria>

  <done>
    A solução compila em Release, o repositório expõe `GetByExternalIdAsync`, `ImportCustomerRow`
    carrega `ExternalId` como último parâmetro e `MapToImportRow` o preenche a partir de `lead.Id`.
    Nenhum arquivo de migration foi criado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste InMemory de GetByExternalIdAsync</name>

  <files>
    api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs
  </files>

  <read_first>
    - api-core/tests/Diax.Tests/Customers/ImportHistoryQueryTests.cs (linhas 1-70 — FakeCurrentUserService + CreateDbContext InMemory, padrão a copiar)
    - api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs (o método recém-criado)
    - api-core/src/Diax.Domain/Customers/Customer.cs (linhas 218-235 e 293-301 — construtor público e SetExternalId)
    - api-core/tests/Diax.Tests/Diax.Tests.csproj (confirmar: Microsoft.EntityFrameworkCore.InMemory presente, FluentAssertions ausente)
  </read_first>

  <behavior>
    - Teste 1 (`GetByExternalIdAsync_ReturnsCustomer_WhenExternalIdMatches`): seed de dois Customers,
      um com `SetExternalId("4242")` e outro com `SetExternalId("9999")`; buscar "4242" devolve o
      primeiro (comparar por `Id`).
    - Teste 2 (`GetByExternalIdAsync_ReturnsNull_WhenNoMatch`): buscar "0000" devolve `null`.
    - Teste 3 (`GetByExternalIdAsync_TrimsInput`): buscar `"  4242  "` devolve o mesmo Customer do
      teste 1 (o repositório normaliza com `Trim()`, espelhando `SetExternalId`).
    - Teste 4 (`GetByExternalIdAsync_IgnoresCustomersWithNullExternalId`): seed de três Customers
      SEM `ExternalId` (todos null — o índice único é filtrado por `WHERE external_id IS NOT NULL`,
      então múltiplos nulls convivem) mais um com "4242"; buscar "4242" devolve exatamente o
      que tem o ExternalId, e a query não lança.
  </behavior>

  <action>
Criar `api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` na namespace
`Diax.Tests.Customers`, copiando o `FakeCurrentUserService` privado e o helper `CreateDbContext`
de `ImportHistoryQueryTests.cs` (trocar o nome do banco InMemory para
`$"customer-externalid-{Guid.NewGuid()}"` para isolamento entre testes).

Helper de seed:
```csharp
private static Customer SeedCustomer(DiaxDbContext db, string name, string email, string? externalId)
{
    var c = new Customer(name, email, PersonType.Individual, LeadSource.Scraping);
    c.SetExternalId(externalId);
    db.Customers.Add(c);
    return c;
}
```

Escrever os 4 `[Fact]` descritos em `<behavior>`, usando **apenas** `Assert.Equal`, `Assert.Null`
e `Assert.NotNull` (xUnit puro — `Should()` NÃO compila neste projeto). Cada teste cria seu
próprio `DiaxDbContext`, chama `await db.SaveChangesAsync()`, instancia
`new CustomerRepository(db)` e chama `await repository.GetByExternalIdAsync(...)`.

Usings necessários: `Diax.Application.Common`, `Diax.Domain.Customers`,
`Diax.Domain.Customers.Enums`, `Diax.Infrastructure.Data`,
`Diax.Infrastructure.Data.Repositories`, `Microsoft.EntityFrameworkCore`, `Xunit`.

⚠️ `Customer` NÃO tem filtro global de query (`CLAUDE.md`: "Customer entity has NO query filter"),
então nenhum setup de UserId é necessário além do que o `DiaxDbContext` exige no construtor.
  </action>

  <verify>
    <automated>cd api-core && dotnet test --filter "FullyQualifiedName~CustomerRepositoryExternalIdTests" -c Release</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo `api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` existe
    - `grep -c "\[Fact\]" api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` retorna 4
    - `grep -q "Should()" api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` é FALSO
    - `grep -q "GetByExternalIdAsync_ReturnsNull_WhenNoMatch" api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` é verdadeiro
    - `grep -q "GetByExternalIdAsync_IgnoresCustomersWithNullExternalId" api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs` é verdadeiro
    - `cd api-core && dotnet test --filter "FullyQualifiedName~CustomerRepositoryExternalIdTests" -c Release` reporta `Passed! ... Failed: 0, Passed: 4`
    - `cd api-core && dotnet test -c Release` reporta `Failed: 0` e um total >= 872 (baseline 868 + 4 novos)
  </acceptance_criteria>

  <done>
    Os 4 testes passam em Release e a suíte completa continua verde com o total crescido em 4.
  </done>
</task>

</tasks>

<verification>
```bash
cd api-core && dotnet build -c Release
cd api-core && dotnet test --filter "FullyQualifiedName~CustomerRepositoryExternalIdTests" -c Release
cd api-core && dotnet test -c Release   # Failed: 0, total >= 872
git status --porcelain api-core/src/Diax.Infrastructure/Data/Migrations/   # deve sair vazio
```
</verification>

<success_criteria>
- `ICustomerRepository.GetByExternalIdAsync` existe e tem implementação EF Core testada
- `ImportCustomerRow.ExternalId` existe como último parâmetro nullable — nenhum chamador posicional quebrou
- `MapToImportRow` propaga `lead.Id` como `ExternalId`
- Zero migrations criadas
- Suíte completa verde, total >= 872 testes
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/08-import-—-dedup-e-score-em-tempo-real/08-01-SUMMARY.md`.

Commit (stagear caminhos EXPLÍCITOS — nunca `git add -A` neste repo):
```bash
git add api-core/src/Diax.Domain/Customers/ICustomerRepository.cs \
        api-core/src/Diax.Infrastructure/Data/Repositories/CustomerRepository.cs \
        api-core/src/Diax.Application/Customers/Dtos/BulkImportDtos.cs \
        api-core/src/Diax.Application/Customers/ExtractorIntegrationService.cs \
        api-core/tests/Diax.Tests/Customers/CustomerRepositoryExternalIdTests.cs
git commit -m "feat(08-01): contratos de dedup por ExternalId (IMPT-01/IMPT-02)"
```
</output>
